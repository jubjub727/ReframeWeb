# ruff: noqa
# flake8: noqa
# pylint: skip-file
# mypy: ignore-errors
# pyright: reportPrivateUsage=false, reportUnknownArgumentType=false
# fmt: off
# isort: skip_file

# ----------------------------------------------------------------------------
#
#  Welcome to Baml! To use this generated code, please run the following:
#
#  $ pip install baml
#
# ----------------------------------------------------------------------------

# This file was generated by BAML: please do not edit it. Instead, edit the
# BAML files and re-generate this code using: baml-cli generate
# baml-cli is available with the baml package.

from __future__ import annotations

import typing
import pydantic

if typing.TYPE_CHECKING:
    from ... import baml
    from ...baml import UNSET as UNSET


class Duration(pydantic.BaseModel):
    _nanoseconds: int
    @staticmethod
    def from_nanoseconds(ns: typing.Union[int, int]) -> Duration: ...
    @staticmethod
    async def from_nanoseconds_async(ns: typing.Union[int, int]) -> Duration: ...
    @staticmethod
    def from_microseconds(us: typing.Union[int, int]) -> Duration: ...
    @staticmethod
    async def from_microseconds_async(us: typing.Union[int, int]) -> Duration: ...
    @staticmethod
    def from_milliseconds(ms: typing.Union[int, int]) -> Duration: ...
    @staticmethod
    async def from_milliseconds_async(ms: typing.Union[int, int]) -> Duration: ...
    @staticmethod
    def from_seconds(s: typing.Union[int, int]) -> Duration: ...
    @staticmethod
    async def from_seconds_async(s: typing.Union[int, int]) -> Duration: ...
    @staticmethod
    def from_minutes(m: typing.Union[int, int]) -> Duration: ...
    @staticmethod
    async def from_minutes_async(m: typing.Union[int, int]) -> Duration: ...
    @staticmethod
    def from_hours(h: typing.Union[int, int]) -> Duration: ...
    @staticmethod
    async def from_hours_async(h: typing.Union[int, int]) -> Duration: ...
    def abs(self) -> Duration:
        """Creates a new `Duration` representing the positive magnitude of `self`."""
    async def abs_async(self) -> Duration:
        """Creates a new `Duration` representing the positive magnitude of `self`."""
    def to_nanoseconds(self) -> int:
        """Returns the number of nanoseconds in `self`."""
    async def to_nanoseconds_async(self) -> int:
        """Returns the number of nanoseconds in `self`."""
    def to_microseconds(self) -> int:
        """Returns the number of microseconds in `self`.
        Lossy: rounds down"""
    async def to_microseconds_async(self) -> int:
        """Returns the number of microseconds in `self`.
        Lossy: rounds down"""
    def to_milliseconds(self) -> int:
        """Returns the number of milliseconds in `self`.
        Lossy: rounds down"""
    async def to_milliseconds_async(self) -> int:
        """Returns the number of milliseconds in `self`.
        Lossy: rounds down"""
    def to_seconds(self) -> int:
        """Returns the number of seconds in `self`.
        Lossy: rounds down"""
    async def to_seconds_async(self) -> int:
        """Returns the number of seconds in `self`.
        Lossy: rounds down"""
    def to_minutes(self) -> int:
        """Returns the number of minutes in `self`.
        Lossy: rounds down"""
    async def to_minutes_async(self) -> int:
        """Returns the number of minutes in `self`.
        Lossy: rounds down"""
    def to_hours(self) -> int:
        """Returns the number of hours in `self`.
        Lossy: rounds down"""
    async def to_hours_async(self) -> int:
        """Returns the number of hours in `self`.
        Lossy: rounds down"""


class Instant(pydantic.BaseModel):
    _nanoseconds: int
    @staticmethod
    def now() -> Instant:
        """Creates a new `Instant` representing the current point in time.

        Note that this uses wall-clock time and is not guaranteed to be monotonic
        (e.g. [NTP](https://en.wikipedia.org/wiki/Network_Time_Protocol) adjustments may cause time to jump backwards).

        ## Panics
        If the system clock is not available, this function will panic."""
    @staticmethod
    async def now_async() -> Instant:
        """Creates a new `Instant` representing the current point in time.

        Note that this uses wall-clock time and is not guaranteed to be monotonic
        (e.g. [NTP](https://en.wikipedia.org/wiki/Network_Time_Protocol) adjustments may cause time to jump backwards).

        ## Panics
        If the system clock is not available, this function will panic."""
    @staticmethod
    def parse(s: str) -> Instant:
        """Raises:
            ParseError"""
    @staticmethod
    async def parse_async(s: str) -> Instant:
        """Raises:
            ParseError"""
    @staticmethod
    def from_json(j: baml.json.json) -> None:
        """Raises:
            JsonDecodeError"""
    @staticmethod
    async def from_json_async(j: baml.json.json) -> None:
        """Raises:
            JsonDecodeError"""
    @staticmethod
    def from_timestamp_nanoseconds(ns: int) -> Instant:
        """Creates a new `Instant` from a number of nanoseconds since the UNIX epoch."""
    @staticmethod
    async def from_timestamp_nanoseconds_async(ns: int) -> Instant:
        """Creates a new `Instant` from a number of nanoseconds since the UNIX epoch."""
    @staticmethod
    def from_timestamp_microseconds(us: int) -> Instant:
        """Creates a new `Instant` from a number of microseconds since the UNIX epoch."""
    @staticmethod
    async def from_timestamp_microseconds_async(us: int) -> Instant:
        """Creates a new `Instant` from a number of microseconds since the UNIX epoch."""
    @staticmethod
    def from_timestamp_milliseconds(ms: int) -> Instant:
        """Creates a new `Instant` from a number of milliseconds since the UNIX epoch."""
    @staticmethod
    async def from_timestamp_milliseconds_async(ms: int) -> Instant:
        """Creates a new `Instant` from a number of milliseconds since the UNIX epoch."""
    @staticmethod
    def from_timestamp_seconds(s: int) -> Instant:
        """Creates a new `Instant` from a number of seconds since the UNIX epoch."""
    @staticmethod
    async def from_timestamp_seconds_async(s: int) -> Instant:
        """Creates a new `Instant` from a number of seconds since the UNIX epoch."""
    @staticmethod
    def epoch() -> Instant:
        """Returns the Unix epoch (1970-01-01T00:00:00Z)."""
    @staticmethod
    async def epoch_async() -> Instant:
        """Returns the Unix epoch (1970-01-01T00:00:00Z)."""
    def elapsed(self) -> Duration:
        """Creates a new `Duration` representing the time elapsed since `self`.
        If `time` is in the future, the duration will be negative.

        Uses `Instant.now()` to get the current time. See it for caveats.

        Also note that since this operation has limited accuracy
        due to measurement overhead, it is not recommended to use this
        for high-precision measurements.

        ## Examples
        ```baml
        let start = Instant.now();
        let response = baml.http.fetch("https://example.com");
        let duration = start.elapsed();
        ```"""
    async def elapsed_async(self) -> Duration:
        """Creates a new `Duration` representing the time elapsed since `self`.
        If `time` is in the future, the duration will be negative.

        Uses `Instant.now()` to get the current time. See it for caveats.

        Also note that since this operation has limited accuracy
        due to measurement overhead, it is not recommended to use this
        for high-precision measurements.

        ## Examples
        ```baml
        let start = Instant.now();
        let response = baml.http.fetch("https://example.com");
        let duration = start.elapsed();
        ```"""
    def to_string(self) -> str: ...
    async def to_string_async(self) -> str: ...
    def _to_string_impl(self) -> str:
        """Internal RFC 3339 formatter. Throws `InvalidArgument` when the year is
        outside the 4-digit RFC 3339 range; `baml.ToString.to_string` turns that
        into a panic, while `to_json` surfaces it as a `JsonSerializationError`.

        Raises:
            InvalidArgument"""
    async def _to_string_impl_async(self) -> str:
        """Internal RFC 3339 formatter. Throws `InvalidArgument` when the year is
        outside the 4-digit RFC 3339 range; `baml.ToString.to_string` turns that
        into a panic, while `to_json` surfaces it as a `JsonSerializationError`.

        Raises:
            InvalidArgument"""
    def to_json(self) -> baml.json.json:
        """Raises:
            JsonSerializationError"""
    async def to_json_async(self) -> baml.json.json:
        """Raises:
            JsonSerializationError"""
    def max(self, other: Instant) -> Instant:
        """If `self` is before `other`, returns `other`. Otherwise, returns `self`."""
    async def max_async(self, other: Instant) -> Instant:
        """If `self` is before `other`, returns `other`. Otherwise, returns `self`."""
    def min(self, other: Instant) -> Instant:
        """If `self` is after `other`, returns `other`. Otherwise, returns `self`."""
    async def min_async(self, other: Instant) -> Instant:
        """If `self` is after `other`, returns `other`. Otherwise, returns `self`."""
    def abs_diff(self, other: Instant) -> Duration:
        """Returns the absolute value of the difference between `self` and `other`.
        This operation is commutative: swapping `self` and `other` does not change the result."""
    async def abs_diff_async(self, other: Instant) -> Duration:
        """Returns the absolute value of the difference between `self` and `other`.
        This operation is commutative: swapping `self` and `other` does not change the result."""
    def to_timestamp_nanoseconds(self) -> int:
        """Returns the number of nanoseconds since the UNIX epoch."""
    async def to_timestamp_nanoseconds_async(self) -> int:
        """Returns the number of nanoseconds since the UNIX epoch."""
    def to_timestamp_microseconds(self) -> int:
        """Returns the number of microseconds since the UNIX epoch.
        Lossy: rounds down"""
    async def to_timestamp_microseconds_async(self) -> int:
        """Returns the number of microseconds since the UNIX epoch.
        Lossy: rounds down"""
    def to_timestamp_milliseconds(self) -> int:
        """Returns the number of milliseconds since the UNIX epoch.
        Lossy: rounds down"""
    async def to_timestamp_milliseconds_async(self) -> int:
        """Returns the number of milliseconds since the UNIX epoch.
        Lossy: rounds down"""
    def to_timestamp_seconds(self) -> int:
        """Returns the number of seconds since the UNIX epoch.
        Lossy: rounds down"""
    async def to_timestamp_seconds_async(self) -> int:
        """Returns the number of seconds since the UNIX epoch.
        Lossy: rounds down"""


class PlainDate(pydantic.BaseModel):
    _days: int
    @staticmethod
    def from_components(year: int, month: int, day: int) -> PlainDate:
        """Raises:
            InvalidArgument"""
    @staticmethod
    async def from_components_async(year: int, month: int, day: int) -> PlainDate:
        """Raises:
            InvalidArgument"""
    @staticmethod
    def parse(s: str) -> PlainDate:
        """Raises:
            ParseError"""
    @staticmethod
    async def parse_async(s: str) -> PlainDate:
        """Raises:
            ParseError"""
    @staticmethod
    def from_json(j: baml.json.json) -> None:
        """Raises:
            JsonDecodeError"""
    @staticmethod
    async def from_json_async(j: baml.json.json) -> None:
        """Raises:
            JsonDecodeError"""
    def to_string(self) -> str: ...
    async def to_string_async(self) -> str: ...
    def _to_string_impl(self) -> str:
        """Internal ISO 8601 formatter. Throws `InvalidArgument` when the year is
        out of range; `baml.ToString.to_string` turns that into a panic, while
        `to_json` surfaces it as a `JsonSerializationError`.

        Raises:
            InvalidArgument"""
    async def _to_string_impl_async(self) -> str:
        """Internal ISO 8601 formatter. Throws `InvalidArgument` when the year is
        out of range; `baml.ToString.to_string` turns that into a panic, while
        `to_json` surfaces it as a `JsonSerializationError`.

        Raises:
            InvalidArgument"""
    def to_plain_datetime(self, *, time: typing.Union[PlainTime, None, UNSET] = None) -> PlainDateTime:
        """Combines with a time-of-day into a `PlainDateTime`. Defaults to
        midnight when `time` is omitted (pass by name to override:
        `d.to_plain_datetime(time = t)`)."""
    async def to_plain_datetime_async(self, *, time: typing.Union[PlainTime, None, UNSET] = None) -> PlainDateTime:
        """Combines with a time-of-day into a `PlainDateTime`. Defaults to
        midnight when `time` is omitted (pass by name to override:
        `d.to_plain_datetime(time = t)`)."""
    def _to_plain_datetime(self, time: typing.Optional[PlainTime]) -> PlainDateTime: ...
    async def _to_plain_datetime_async(self, time: typing.Optional[PlainTime]) -> PlainDateTime: ...
    def year(self) -> int:
        """The calendar year.

        Raises:
            InvalidArgument"""
    async def year_async(self) -> int:
        """The calendar year.

        Raises:
            InvalidArgument"""
    def month(self) -> int:
        """The calendar month, in `[1, 12]`.

        Raises:
            InvalidArgument"""
    async def month_async(self) -> int:
        """The calendar month, in `[1, 12]`.

        Raises:
            InvalidArgument"""
    def day(self) -> int:
        """The day of the month, in `[1, 31]`.

        Raises:
            InvalidArgument"""
    async def day_async(self) -> int:
        """The day of the month, in `[1, 31]`.

        Raises:
            InvalidArgument"""
    def to_json(self) -> baml.json.json:
        """Raises:
            JsonSerializationError"""
    async def to_json_async(self) -> baml.json.json:
        """Raises:
            JsonSerializationError"""


class PlainDateTime(pydantic.BaseModel):
    _nanoseconds: int
    @staticmethod
    def from_components(year: int, month: int, day: int, *, hour: typing.Union[int, UNSET] = 0, minute: typing.Union[int, UNSET] = 0, second: typing.Union[int, UNSET] = 0, millisecond: typing.Union[int, UNSET] = 0, microsecond: typing.Union[int, UNSET] = 0, nanosecond: typing.Union[int, UNSET] = 0) -> PlainDateTime:
        """Creates a `PlainDateTime` from calendar/clock components. `month` and
        `day` are 1-based. Defaulted clock components are passed by name:
        `PlainDateTime.from_components(1979, 5, 27, hour = 7)`.

        Throws `root.errors.InvalidArgument` if a component is out of range
        (e.g. month 13, Feb 30) or the year is outside ±9999.

        Raises:
            InvalidArgument"""
    @staticmethod
    async def from_components_async(year: int, month: int, day: int, *, hour: typing.Union[int, UNSET] = 0, minute: typing.Union[int, UNSET] = 0, second: typing.Union[int, UNSET] = 0, millisecond: typing.Union[int, UNSET] = 0, microsecond: typing.Union[int, UNSET] = 0, nanosecond: typing.Union[int, UNSET] = 0) -> PlainDateTime:
        """Creates a `PlainDateTime` from calendar/clock components. `month` and
        `day` are 1-based. Defaulted clock components are passed by name:
        `PlainDateTime.from_components(1979, 5, 27, hour = 7)`.

        Throws `root.errors.InvalidArgument` if a component is out of range
        (e.g. month 13, Feb 30) or the year is outside ±9999.

        Raises:
            InvalidArgument"""
    @staticmethod
    def _from_components(year: int, month: int, day: int, hour: int, minute: int, second: int, millisecond: int, microsecond: int, nanosecond: int) -> PlainDateTime:
        """Raises:
            InvalidArgument"""
    @staticmethod
    async def _from_components_async(year: int, month: int, day: int, hour: int, minute: int, second: int, millisecond: int, microsecond: int, nanosecond: int) -> PlainDateTime:
        """Raises:
            InvalidArgument"""
    @staticmethod
    def parse(s: str) -> PlainDateTime:
        """Raises:
            ParseError"""
    @staticmethod
    async def parse_async(s: str) -> PlainDateTime:
        """Raises:
            ParseError"""
    @staticmethod
    def from_json(j: baml.json.json) -> None:
        """Raises:
            JsonDecodeError"""
    @staticmethod
    async def from_json_async(j: baml.json.json) -> None:
        """Raises:
            JsonDecodeError"""
    def to_string(self) -> str: ...
    async def to_string_async(self) -> str: ...
    def _to_string_impl(self) -> str:
        """Internal ISO 8601 formatter. Throws `InvalidArgument` when the year is
        outside ±9999; `baml.ToString.to_string` turns that into a panic, while
        `to_json` surfaces it as a `JsonSerializationError`.

        Raises:
            InvalidArgument"""
    async def _to_string_impl_async(self) -> str:
        """Internal ISO 8601 formatter. Throws `InvalidArgument` when the year is
        outside ±9999; `baml.ToString.to_string` turns that into a panic, while
        `to_json` surfaces it as a `JsonSerializationError`.

        Raises:
            InvalidArgument"""
    def to_zoned(self, timezone: typing.Union[TimeZoneOffset, str], *, disambiguation: typing.Union[typing.Literal["compatible"], typing.Literal["earlier"], typing.Literal["later"], typing.Literal["reject"], UNSET] = "compatible") -> ZonedDateTime:
        """Locates this wall-clock reading in a timezone, producing an absolute
        time. With a `TimeZoneOffset` the conversion is exact. With an IANA
        identifier, DST gaps/overlaps are resolved per `disambiguation`
        (passed by name: `dt.to_zoned(tz, disambiguation = "earlier")`).

        Raises:
            UnknownTimezoneError, AmbiguousTimeError, Io"""
    async def to_zoned_async(self, timezone: typing.Union[TimeZoneOffset, str], *, disambiguation: typing.Union[typing.Literal["compatible"], typing.Literal["earlier"], typing.Literal["later"], typing.Literal["reject"], UNSET] = "compatible") -> ZonedDateTime:
        """Locates this wall-clock reading in a timezone, producing an absolute
        time. With a `TimeZoneOffset` the conversion is exact. With an IANA
        identifier, DST gaps/overlaps are resolved per `disambiguation`
        (passed by name: `dt.to_zoned(tz, disambiguation = "earlier")`).

        Raises:
            UnknownTimezoneError, AmbiguousTimeError, Io"""
    def max(self, other: PlainDateTime) -> PlainDateTime:
        """If `self` is before `other` (civil comparison), returns `other`.
        Otherwise, returns `self`."""
    async def max_async(self, other: PlainDateTime) -> PlainDateTime:
        """If `self` is before `other` (civil comparison), returns `other`.
        Otherwise, returns `self`."""
    def min(self, other: PlainDateTime) -> PlainDateTime:
        """If `self` is after `other` (civil comparison), returns `other`.
        Otherwise, returns `self`."""
    async def min_async(self, other: PlainDateTime) -> PlainDateTime:
        """If `self` is after `other` (civil comparison), returns `other`.
        Otherwise, returns `self`."""
    def to_plain_date(self) -> PlainDate:
        """Raises:
            InvalidArgument"""
    async def to_plain_date_async(self) -> PlainDate:
        """Raises:
            InvalidArgument"""
    def to_plain_time(self) -> PlainTime: ...
    async def to_plain_time_async(self) -> PlainTime: ...
    def year(self) -> int:
        """The calendar year.

        Raises:
            InvalidArgument"""
    async def year_async(self) -> int:
        """The calendar year.

        Raises:
            InvalidArgument"""
    def month(self) -> int:
        """The calendar month, in `[1, 12]`.

        Raises:
            InvalidArgument"""
    async def month_async(self) -> int:
        """The calendar month, in `[1, 12]`.

        Raises:
            InvalidArgument"""
    def day(self) -> int:
        """The day of the month, in `[1, 31]`.

        Raises:
            InvalidArgument"""
    async def day_async(self) -> int:
        """The day of the month, in `[1, 31]`.

        Raises:
            InvalidArgument"""
    def hour(self) -> int:
        """The hour of the day, in `[0, 23]`.

        Raises:
            InvalidArgument"""
    async def hour_async(self) -> int:
        """The hour of the day, in `[0, 23]`.

        Raises:
            InvalidArgument"""
    def minute(self) -> int:
        """The minute of the hour, in `[0, 59]`.

        Raises:
            InvalidArgument"""
    async def minute_async(self) -> int:
        """The minute of the hour, in `[0, 59]`.

        Raises:
            InvalidArgument"""
    def second(self) -> int:
        """The second of the minute, in `[0, 59]`.

        Raises:
            InvalidArgument"""
    async def second_async(self) -> int:
        """The second of the minute, in `[0, 59]`.

        Raises:
            InvalidArgument"""
    def millisecond(self) -> int:
        """The millisecond of the second, in `[0, 999]`.

        Raises:
            InvalidArgument"""
    async def millisecond_async(self) -> int:
        """The millisecond of the second, in `[0, 999]`.

        Raises:
            InvalidArgument"""
    def to_json(self) -> baml.json.json:
        """Raises:
            JsonSerializationError"""
    async def to_json_async(self) -> baml.json.json:
        """Raises:
            JsonSerializationError"""


class PlainTime(pydantic.BaseModel):
    _nanoseconds: int
    @staticmethod
    def from_components(hour: int, *, minute: typing.Union[int, UNSET] = 0, second: typing.Union[int, UNSET] = 0, millisecond: typing.Union[int, UNSET] = 0, microsecond: typing.Union[int, UNSET] = 0, nanosecond: typing.Union[int, UNSET] = 0) -> PlainTime:
        """Creates a `PlainTime` from clock components. Defaulted components
        are passed by name: `PlainTime.from_components(7, minute = 32)`.

        Throws `root.errors.InvalidArgument` if a component is out of range
        (e.g. hour 24, minute 60).

        Raises:
            InvalidArgument"""
    @staticmethod
    async def from_components_async(hour: int, *, minute: typing.Union[int, UNSET] = 0, second: typing.Union[int, UNSET] = 0, millisecond: typing.Union[int, UNSET] = 0, microsecond: typing.Union[int, UNSET] = 0, nanosecond: typing.Union[int, UNSET] = 0) -> PlainTime:
        """Creates a `PlainTime` from clock components. Defaulted components
        are passed by name: `PlainTime.from_components(7, minute = 32)`.

        Throws `root.errors.InvalidArgument` if a component is out of range
        (e.g. hour 24, minute 60).

        Raises:
            InvalidArgument"""
    @staticmethod
    def _from_components(hour: int, minute: int, second: int, millisecond: int, microsecond: int, nanosecond: int) -> PlainTime:
        """Raises:
            InvalidArgument"""
    @staticmethod
    async def _from_components_async(hour: int, minute: int, second: int, millisecond: int, microsecond: int, nanosecond: int) -> PlainTime:
        """Raises:
            InvalidArgument"""
    @staticmethod
    def parse(s: str) -> PlainTime:
        """Raises:
            ParseError"""
    @staticmethod
    async def parse_async(s: str) -> PlainTime:
        """Raises:
            ParseError"""
    @staticmethod
    def from_json(j: baml.json.json) -> None:
        """Raises:
            JsonDecodeError"""
    @staticmethod
    async def from_json_async(j: baml.json.json) -> None:
        """Raises:
            JsonDecodeError"""
    def to_string(self) -> str: ...
    async def to_string_async(self) -> str: ...
    def _to_string_impl(self) -> str: ...
    async def _to_string_impl_async(self) -> str: ...
    def to_plain_datetime(self, date: PlainDate) -> PlainDateTime:
        """Combines with a date into a `PlainDateTime`."""
    async def to_plain_datetime_async(self, date: PlainDate) -> PlainDateTime:
        """Combines with a date into a `PlainDateTime`."""
    def hour(self) -> int:
        """The hour of the day, in `[0, 23]`."""
    async def hour_async(self) -> int:
        """The hour of the day, in `[0, 23]`."""
    def minute(self) -> int:
        """The minute of the hour, in `[0, 59]`."""
    async def minute_async(self) -> int:
        """The minute of the hour, in `[0, 59]`."""
    def second(self) -> int:
        """The second of the minute, in `[0, 59]`."""
    async def second_async(self) -> int:
        """The second of the minute, in `[0, 59]`."""
    def millisecond(self) -> int:
        """The millisecond of the second, in `[0, 999]`."""
    async def millisecond_async(self) -> int:
        """The millisecond of the second, in `[0, 999]`."""
    def to_json(self) -> baml.json.json:
        """Raises:
            JsonSerializationError"""
    async def to_json_async(self) -> baml.json.json:
        """Raises:
            JsonSerializationError"""


Disambiguation: typing.TypeAlias = typing.Union[typing.Literal["compatible"], typing.Literal["earlier"], typing.Literal["later"], typing.Literal["reject"]]


class UnknownTimezoneError(pydantic.BaseModel):
    timezone: str


class AmbiguousTimeError(pydantic.BaseModel):
    message: str


class TimeZoneOffset(pydantic.BaseModel):
    _nanoseconds: int
    @staticmethod
    def utc() -> TimeZoneOffset:
        """The UTC (zero) offset."""
    @staticmethod
    async def utc_async() -> TimeZoneOffset:
        """The UTC (zero) offset."""
    @staticmethod
    def new(hours: int, minutes: int) -> TimeZoneOffset:
        """Creates a `TimeZoneOffset` from hours and minutes east of UTC.
        Both components must carry the same sign (e.g. `new(-7, 0)`,
        `new(5, 30)`, `new(-9, -30)`).

        Throws `root.errors.InvalidArgument` if the signs differ or the
        result exceeds ±24 hours.

        Raises:
            InvalidArgument"""
    @staticmethod
    async def new_async(hours: int, minutes: int) -> TimeZoneOffset:
        """Creates a `TimeZoneOffset` from hours and minutes east of UTC.
        Both components must carry the same sign (e.g. `new(-7, 0)`,
        `new(5, 30)`, `new(-9, -30)`).

        Throws `root.errors.InvalidArgument` if the signs differ or the
        result exceeds ±24 hours.

        Raises:
            InvalidArgument"""
    @staticmethod
    def from_timezone(timezone: str, at: Instant) -> TimeZoneOffset:
        """Resolves an IANA timezone identifier (e.g. `"America/Los_Angeles"`)
        to its concrete offset at the absolute time `at` (DST-aware).

        Resolution uses the host's timezone database.

        Raises:
            UnknownTimezoneError, Io"""
    @staticmethod
    async def from_timezone_async(timezone: str, at: Instant) -> TimeZoneOffset:
        """Resolves an IANA timezone identifier (e.g. `"America/Los_Angeles"`)
        to its concrete offset at the absolute time `at` (DST-aware).

        Resolution uses the host's timezone database.

        Raises:
            UnknownTimezoneError, Io"""
    @staticmethod
    def local() -> TimeZoneOffset:
        """Returns the local timezone offset right now.
        Note that even in the same location, this may vary over time based on
        daylight savings.

        Raises:
            UnknownTimezoneError, Io"""
    @staticmethod
    async def local_async() -> TimeZoneOffset:
        """Returns the local timezone offset right now.
        Note that even in the same location, this may vary over time based on
        daylight savings.

        Raises:
            UnknownTimezoneError, Io"""
    @staticmethod
    def from_duration(duration: Duration) -> TimeZoneOffset:
        """Raises:
            InvalidArgument"""
    @staticmethod
    async def from_duration_async(duration: Duration) -> TimeZoneOffset:
        """Raises:
            InvalidArgument"""
    def to_duration(self) -> Duration: ...
    async def to_duration_async(self) -> Duration: ...
    def hours(self) -> int:
        """The hour component of the offset. Rounded toward zero."""
    async def hours_async(self) -> int:
        """The hour component of the offset. Rounded toward zero."""
    def minutes(self) -> int:
        """The minute component of the offset, modulo one hour. Rounded toward zero."""
    async def minutes_async(self) -> int:
        """The minute component of the offset, modulo one hour. Rounded toward zero."""


def system_timezone() -> str:
    """Returns the system's IANA timezone identifier, for example
    `"America/Los_Angeles"`. Mirrors `Temporal.Now.timeZoneId()`.

    An IO function so hosts can swap the system-state source out.
    Throws if the host cannot determine its timezone.

    Raises:
        Io"""
async def system_timezone_async() -> str:
    """Returns the system's IANA timezone identifier, for example
    `"America/Los_Angeles"`. Mirrors `Temporal.Now.timeZoneId()`.

    An IO function so hosts can swap the system-state source out.
    Throws if the host cannot determine its timezone.

    Raises:
        Io"""


def _tz_offset_at(timezone: str, at_ns: int) -> typing.Optional[int]:
    """Internal: resolves an IANA identifier to its offset (in nanoseconds) at
    the absolute time `at_ns` (nanoseconds since the Unix epoch), using the
    host's timezone database. Returns `null` if the identifier is unknown.

    Raises:
        Io"""
async def _tz_offset_at_async(timezone: str, at_ns: int) -> typing.Optional[int]:
    """Internal: resolves an IANA identifier to its offset (in nanoseconds) at
    the absolute time `at_ns` (nanoseconds since the Unix epoch), using the
    host's timezone database. Returns `null` if the identifier is unknown.

    Raises:
        Io"""


def _tz_to_instant(timezone: str, civil_ns: int, disambiguation: str) -> typing.Optional[int]:
    """Internal: locates the civil ("wall-clock") reading `civil_ns` (nanoseconds
    since 1970-01-01T00:00:00, interpreted as if UTC) in the IANA timezone
    `timezone`, resolving DST gaps/overlaps per `disambiguation` (which must
    be `"compatible"`, `"earlier"`, or `"later"` — `"reject"` is implemented
    by callers as: resolve both `"earlier"` and `"later"` and require them to
    agree). Returns the resolved absolute time as nanoseconds since the Unix
    epoch, or `null` if the identifier is unknown to the host's timezone
    database.

    Raises:
        Io"""
async def _tz_to_instant_async(timezone: str, civil_ns: int, disambiguation: str) -> typing.Optional[int]:
    """Internal: locates the civil ("wall-clock") reading `civil_ns` (nanoseconds
    since 1970-01-01T00:00:00, interpreted as if UTC) in the IANA timezone
    `timezone`, resolving DST gaps/overlaps per `disambiguation` (which must
    be `"compatible"`, `"earlier"`, or `"later"` — `"reject"` is implemented
    by callers as: resolve both `"earlier"` and `"later"` and require them to
    agree). Returns the resolved absolute time as nanoseconds since the Unix
    epoch, or `null` if the identifier is unknown to the host's timezone
    database.

    Raises:
        Io"""


class ZonedDateTime(pydantic.BaseModel):
    _nanoseconds: int
    _offset_ns: typing.Optional[int]
    _iana: typing.Optional[str]
    @staticmethod
    def now() -> ZonedDateTime:
        """Creates a new `ZonedDateTime` with the current time in the system
        timezone (an IANA identifier, e.g. `"America/Los_Angeles"`).
        Mirrors `Temporal.Now.zonedDateTimeISO()`.

        Raises:
            Io"""
    @staticmethod
    async def now_async() -> ZonedDateTime:
        """Creates a new `ZonedDateTime` with the current time in the system
        timezone (an IANA identifier, e.g. `"America/Los_Angeles"`).
        Mirrors `Temporal.Now.zonedDateTimeISO()`.

        Raises:
            Io"""
    @staticmethod
    def now_in(timezone: typing.Union[TimeZoneOffset, str]) -> ZonedDateTime:
        """Creates a new `ZonedDateTime` with the current time in the given
        timezone."""
    @staticmethod
    async def now_in_async(timezone: typing.Union[TimeZoneOffset, str]) -> ZonedDateTime:
        """Creates a new `ZonedDateTime` with the current time in the given
        timezone."""
    @staticmethod
    def from_instant(instant: Instant, timezone: typing.Union[TimeZoneOffset, str]) -> ZonedDateTime:
        """Pairs an absolute time with a timezone."""
    @staticmethod
    async def from_instant_async(instant: Instant, timezone: typing.Union[TimeZoneOffset, str]) -> ZonedDateTime:
        """Pairs an absolute time with a timezone."""
    @staticmethod
    def from_components(timezone: typing.Union[TimeZoneOffset, str], year: int, month: int, day: int, *, hour: typing.Union[int, UNSET] = 0, minute: typing.Union[int, UNSET] = 0, second: typing.Union[int, UNSET] = 0, millisecond: typing.Union[int, UNSET] = 0, microsecond: typing.Union[int, UNSET] = 0, nanosecond: typing.Union[int, UNSET] = 0, disambiguation: typing.Union[typing.Literal["compatible"], typing.Literal["earlier"], typing.Literal["later"], typing.Literal["reject"], UNSET] = "compatible") -> ZonedDateTime:
        """Creates a `ZonedDateTime` from calendar/clock components read in
        `timezone`. `month` and `day` are 1-based. Defaulted clock components
        and `disambiguation` are passed by name. With an IANA timezone, DST
        gaps/overlaps are resolved per `disambiguation` (see `Disambiguation`).

        Raises:
            InvalidArgument, UnknownTimezoneError, AmbiguousTimeError, Io"""
    @staticmethod
    async def from_components_async(timezone: typing.Union[TimeZoneOffset, str], year: int, month: int, day: int, *, hour: typing.Union[int, UNSET] = 0, minute: typing.Union[int, UNSET] = 0, second: typing.Union[int, UNSET] = 0, millisecond: typing.Union[int, UNSET] = 0, microsecond: typing.Union[int, UNSET] = 0, nanosecond: typing.Union[int, UNSET] = 0, disambiguation: typing.Union[typing.Literal["compatible"], typing.Literal["earlier"], typing.Literal["later"], typing.Literal["reject"], UNSET] = "compatible") -> ZonedDateTime:
        """Creates a `ZonedDateTime` from calendar/clock components read in
        `timezone`. `month` and `day` are 1-based. Defaulted clock components
        and `disambiguation` are passed by name. With an IANA timezone, DST
        gaps/overlaps are resolved per `disambiguation` (see `Disambiguation`).

        Raises:
            InvalidArgument, UnknownTimezoneError, AmbiguousTimeError, Io"""
    @staticmethod
    def parse(s: str) -> ZonedDateTime:
        """Raises:
            ParseError"""
    @staticmethod
    async def parse_async(s: str) -> ZonedDateTime:
        """Raises:
            ParseError"""
    @staticmethod
    def from_json(j: baml.json.json) -> None:
        """Raises:
            JsonDecodeError"""
    @staticmethod
    async def from_json_async(j: baml.json.json) -> None:
        """Raises:
            JsonDecodeError"""
    def to_instant(self) -> Instant:
        """The absolute time, dropping the timezone."""
    async def to_instant_async(self) -> Instant:
        """The absolute time, dropping the timezone."""
    def to_string(self) -> str: ...
    async def to_string_async(self) -> str: ...
    def _to_string_impl(self) -> str:
        """Internal RFC 9557 / RFC 3339 formatter. Throws when the value cannot be
        formatted; `baml.ToString.to_string` turns that into a panic, while
        `to_json` surfaces it as a `JsonSerializationError`.

        Raises:
            InvalidArgument, UnknownTimezoneError, Io"""
    async def _to_string_impl_async(self) -> str:
        """Internal RFC 9557 / RFC 3339 formatter. Throws when the value cannot be
        formatted; `baml.ToString.to_string` turns that into a panic, while
        `to_json` surfaces it as a `JsonSerializationError`.

        Raises:
            InvalidArgument, UnknownTimezoneError, Io"""
    def max(self, other: ZonedDateTime) -> ZonedDateTime:
        """If `self` is before `other` (absolute-time comparison), returns
        `other`. Otherwise, returns `self`."""
    async def max_async(self, other: ZonedDateTime) -> ZonedDateTime:
        """If `self` is before `other` (absolute-time comparison), returns
        `other`. Otherwise, returns `self`."""
    def min(self, other: ZonedDateTime) -> ZonedDateTime:
        """If `self` is after `other` (absolute-time comparison), returns
        `other`. Otherwise, returns `self`."""
    async def min_async(self, other: ZonedDateTime) -> ZonedDateTime:
        """If `self` is after `other` (absolute-time comparison), returns
        `other`. Otherwise, returns `self`."""
    def with_timezone(self, timezone: typing.Union[TimeZoneOffset, str]) -> ZonedDateTime:
        """Same absolute time, different timezone."""
    async def with_timezone_async(self, timezone: typing.Union[TimeZoneOffset, str]) -> ZonedDateTime:
        """Same absolute time, different timezone."""
    def timezone(self) -> typing.Union[TimeZoneOffset, str]:
        """The timezone: a `TimeZoneOffset` if fixed, or the IANA identifier."""
    async def timezone_async(self) -> typing.Union[TimeZoneOffset, str]:
        """The timezone: a `TimeZoneOffset` if fixed, or the IANA identifier."""
    def timezone_offset(self) -> TimeZoneOffset:
        """The resolved `TimeZoneOffset` for this `ZonedDateTime`. If the
        timezone is an IANA identifier, it is resolved to a concrete offset
        based on the absolute time (DST-aware), using the host's timezone
        database.

        Raises:
            UnknownTimezoneError, Io"""
    async def timezone_offset_async(self) -> TimeZoneOffset:
        """The resolved `TimeZoneOffset` for this `ZonedDateTime`. If the
        timezone is an IANA identifier, it is resolved to a concrete offset
        based on the absolute time (DST-aware), using the host's timezone
        database.

        Raises:
            UnknownTimezoneError, Io"""
    def to_plain(self) -> PlainDateTime:
        """Drops the timezone, keeping the wall-clock reading.

        Raises:
            UnknownTimezoneError, Io"""
    async def to_plain_async(self) -> PlainDateTime:
        """Drops the timezone, keeping the wall-clock reading.

        Raises:
            UnknownTimezoneError, Io"""
    def year(self) -> int:
        """The calendar year, resolved through the timezone.

        Raises:
            InvalidArgument, UnknownTimezoneError, Io"""
    async def year_async(self) -> int:
        """The calendar year, resolved through the timezone.

        Raises:
            InvalidArgument, UnknownTimezoneError, Io"""
    def month(self) -> int:
        """The calendar month, in `[1, 12]`, resolved through the timezone.

        Raises:
            InvalidArgument, UnknownTimezoneError, Io"""
    async def month_async(self) -> int:
        """The calendar month, in `[1, 12]`, resolved through the timezone.

        Raises:
            InvalidArgument, UnknownTimezoneError, Io"""
    def day(self) -> int:
        """The day of the month, in `[1, 31]`, resolved through the timezone.

        Raises:
            InvalidArgument, UnknownTimezoneError, Io"""
    async def day_async(self) -> int:
        """The day of the month, in `[1, 31]`, resolved through the timezone.

        Raises:
            InvalidArgument, UnknownTimezoneError, Io"""
    def hour(self) -> int:
        """The hour of the day, in `[0, 23]`, resolved through the timezone.

        Raises:
            InvalidArgument, UnknownTimezoneError, Io"""
    async def hour_async(self) -> int:
        """The hour of the day, in `[0, 23]`, resolved through the timezone.

        Raises:
            InvalidArgument, UnknownTimezoneError, Io"""
    def minute(self) -> int:
        """The minute of the hour, in `[0, 59]`, resolved through the timezone.

        Raises:
            InvalidArgument, UnknownTimezoneError, Io"""
    async def minute_async(self) -> int:
        """The minute of the hour, in `[0, 59]`, resolved through the timezone.

        Raises:
            InvalidArgument, UnknownTimezoneError, Io"""
    def second(self) -> int:
        """The second of the minute, in `[0, 59]`, resolved through the timezone.

        Raises:
            InvalidArgument, UnknownTimezoneError, Io"""
    async def second_async(self) -> int:
        """The second of the minute, in `[0, 59]`, resolved through the timezone.

        Raises:
            InvalidArgument, UnknownTimezoneError, Io"""
    def millisecond(self) -> int:
        """The millisecond of the second, in `[0, 999]`, resolved through the
        timezone.

        Raises:
            InvalidArgument, UnknownTimezoneError, Io"""
    async def millisecond_async(self) -> int:
        """The millisecond of the second, in `[0, 999]`, resolved through the
        timezone.

        Raises:
            InvalidArgument, UnknownTimezoneError, Io"""
    def to_json(self) -> baml.json.json:
        """Raises:
            JsonSerializationError"""
    async def to_json_async(self) -> baml.json.json:
        """Raises:
            JsonSerializationError"""


def _format_zoned(epoch_ns: int, offset_ns: int, iana: typing.Optional[str]) -> str:
    """Internal: formats an absolute time as RFC 3339 at the given offset,
    appending an RFC 9557 `[iana]` annotation when `iana` is non-null.

    Raises:
        InvalidArgument"""
async def _format_zoned_async(epoch_ns: int, offset_ns: int, iana: typing.Optional[str]) -> str:
    """Internal: formats an absolute time as RFC 3339 at the given offset,
    appending an RFC 9557 `[iana]` annotation when `iana` is non-null.

    Raises:
        InvalidArgument"""


__all__ = [
    "Duration",
    "Instant",
    "PlainDate",
    "PlainDateTime",
    "PlainTime",
    "Disambiguation",
    "UnknownTimezoneError",
    "AmbiguousTimeError",
    "TimeZoneOffset",
    "system_timezone",
    "system_timezone_async",
    "ZonedDateTime",
]
