# ruff: noqa
# flake8: noqa
# pylint: skip-file
# mypy: ignore-errors
# pyright: reportPrivateUsage=false, reportUnknownArgumentType=false
# fmt: off
# isort: skip_file

# ----------------------------------------------------------------------------
#
#  Welcome to Baml! To use this generated code, please run the following:
#
#  $ pip install baml
#
# ----------------------------------------------------------------------------

# This file was generated by BAML: please do not edit it. Instead, edit the
# BAML files and re-generate this code using: baml-cli generate
# baml-cli is available with the baml package.

from __future__ import annotations

from . import csv
from . import env
from . import errors
from . import fs
from . import future
from . import glob
from . import host
from . import http
from . import id
from . import io
from . import iter
from . import json
from . import llm
from . import math
from . import media
from . import net
from . import ops
from . import panics
from . import spawn
from . import stream
from . import sys
from . import time
from . import toml
from . import yaml

import typing
import pydantic

if typing.TYPE_CHECKING:
    from .. import baml

from baml_core import BamlError as BamlError, BamlPanic as BamlPanic, UNSET as UNSET


A = typing.TypeVar("A")
E = typing.TypeVar("E")
K = typing.TypeVar("K")
T = typing.TypeVar("T")
U = typing.TypeVar("U")
V = typing.TypeVar("V")


class Bigint(pydantic.BaseModel):
    @staticmethod
    def parse(text: str) -> int:
        """Parses `text` as a base-ten signed integer.
        
        Accepts an optional leading `+` or `-` sign followed by one or more
        ASCII digits. No surrounding whitespace, no underscore separators,
        no hex / octal / binary prefix (`0x` / `0o` / `0b`), no Unicode digits,
        no scientific notation. Callers needing those should preprocess the
        string.
        
        Throws `ParseError` if `text` is empty or contains a non-digit character.
        
        # Examples
        ```
        bigint.parse("42")       // 42n
        bigint.parse("-7")       // -7n
        bigint.parse("+0")       // 0n
        bigint.parse("")         // throws — empty
        bigint.parse("12a")      // throws — non-digit
        bigint.parse("0x2a")     // throws — hex prefix not accepted
        bigint.parse("1_000")    // throws — underscores not accepted
        bigint.parse(" 5 ")      // throws — whitespace not allowed; trim first
        bigint.parse("99999999999999999999")  // 99999999999999999999n
        ```
        
        Raises:
            ParseError"""
    @staticmethod
    async def parse_async(text: str) -> int:
        """Parses `text` as a base-ten signed integer.
        
        Accepts an optional leading `+` or `-` sign followed by one or more
        ASCII digits. No surrounding whitespace, no underscore separators,
        no hex / octal / binary prefix (`0x` / `0o` / `0b`), no Unicode digits,
        no scientific notation. Callers needing those should preprocess the
        string.
        
        Throws `ParseError` if `text` is empty or contains a non-digit character.
        
        # Examples
        ```
        bigint.parse("42")       // 42n
        bigint.parse("-7")       // -7n
        bigint.parse("+0")       // 0n
        bigint.parse("")         // throws — empty
        bigint.parse("12a")      // throws — non-digit
        bigint.parse("0x2a")     // throws — hex prefix not accepted
        bigint.parse("1_000")    // throws — underscores not accepted
        bigint.parse(" 5 ")      // throws — whitespace not allowed; trim first
        bigint.parse("99999999999999999999")  // 99999999999999999999n
        ```
        
        Raises:
            ParseError"""
    @staticmethod
    def random(lower: int, upper: int) -> int:
        """Returns a uniformly distributed random integer in the half-open range
        `[lower, upper)`.
        
        Throws `InvalidArgument` if `lower >= upper` (the range would be empty).
        Uses the host's cryptographic entropy source — no user-supplied RNG
        handle.
        
        # Examples
        ```
        bigint.random(0n, 10n)         // some value in {0, 1, ..., 9}
        bigint.random(-5n, 5n)         // some value in {-5, -4, ..., 4}
        bigint.random(0n, 1n)          // always 0  (single-element range)
        bigint.random(5n, 5n)          // throws — empty range
        bigint.random(10n, 0n)         // throws — lower > upper
        ```
        
        Raises:
            InvalidArgument"""
    @staticmethod
    async def random_async(lower: int, upper: int) -> int:
        """Returns a uniformly distributed random integer in the half-open range
        `[lower, upper)`.
        
        Throws `InvalidArgument` if `lower >= upper` (the range would be empty).
        Uses the host's cryptographic entropy source — no user-supplied RNG
        handle.
        
        # Examples
        ```
        bigint.random(0n, 10n)         // some value in {0, 1, ..., 9}
        bigint.random(-5n, 5n)         // some value in {-5, -4, ..., 4}
        bigint.random(0n, 1n)          // always 0  (single-element range)
        bigint.random(5n, 5n)          // throws — empty range
        bigint.random(10n, 0n)         // throws — lower > upper
        ```
        
        Raises:
            InvalidArgument"""
    def abs(self) -> int:
        """Returns the absolute value of `self`.
        # Examples
        ```
        (-7n).abs()         // 7n
        (3n).abs()          // 3n
        (0n).abs()          // 0n
        ```"""
    async def abs_async(self) -> int:
        """Returns the absolute value of `self`.
        # Examples
        ```
        (-7n).abs()         // 7n
        (3n).abs()          // 3n
        (0n).abs()          // 0n
        ```"""
    def min(self, other: int) -> int:
        """Returns the smaller of `self` and `other`.
        
        # Examples
        ```
        (3n).min(5n)   // 3n
        (3n).min(3)   // 3n (note we cast up from `int` to `bigint` here)
        (-2n).min(0n)  // -2n
        ```"""
    async def min_async(self, other: int) -> int:
        """Returns the smaller of `self` and `other`.
        
        # Examples
        ```
        (3n).min(5n)   // 3n
        (3n).min(3)   // 3n (note we cast up from `int` to `bigint` here)
        (-2n).min(0n)  // -2n
        ```"""
    def max(self, other: int) -> int:
        """Returns the larger of `self` and `other`.
        
        # Examples
        ```
        (3n).max(5n)   // 5n
        (3n).max(3)   // 3n (note we cast up from `int` to `bigint` here)
        (-2n).max(0n)  // 0n
        ```"""
    async def max_async(self, other: int) -> int:
        """Returns the larger of `self` and `other`.
        
        # Examples
        ```
        (3n).max(5n)   // 5n
        (3n).max(3)   // 3n (note we cast up from `int` to `bigint` here)
        (-2n).max(0n)  // 0n
        ```"""
    def clamp(self, min: int, max: int) -> int:
        """Clamps `self` into the range `[min, max]`.
        
        Equivalent to `self.min(max).max(min)`. Callers should pass `min <= max`;
        if `min > max` the result is always `min` (the lower-clamp wins because
        it runs after the upper-clamp).
        
        # Examples
        ```
        (5n).clamp(0n, 10n)    // 5n
        (-3n).clamp(0, 10)   // 0n (note we cast up from `int` to `bigint` here)
        (15n).clamp(0n, 10n)   // 10n
        ```"""
    async def clamp_async(self, min: int, max: int) -> int:
        """Clamps `self` into the range `[min, max]`.
        
        Equivalent to `self.min(max).max(min)`. Callers should pass `min <= max`;
        if `min > max` the result is always `min` (the lower-clamp wins because
        it runs after the upper-clamp).
        
        # Examples
        ```
        (5n).clamp(0n, 10n)    // 5n
        (-3n).clamp(0, 10)   // 0n (note we cast up from `int` to `bigint` here)
        (15n).clamp(0n, 10n)   // 10n
        ```"""
    def isqrt(self) -> int:
        """Returns the integer square root of `self` — that is, the largest `bigint`
        `r` such that `r * r <= self`.
        
        Throws `InvalidArgument` if `self` is negative.
        
        # Examples
        ```
        (10n).isqrt()   // 3n
        (16n).isqrt()   // 4n
        (0n).isqrt()    // 0n
        (-1n).isqrt()   // throws
        ```
        
        Raises:
            InvalidArgument"""
    async def isqrt_async(self) -> int:
        """Returns the integer square root of `self` — that is, the largest `bigint`
        `r` such that `r * r <= self`.
        
        Throws `InvalidArgument` if `self` is negative.
        
        # Examples
        ```
        (10n).isqrt()   // 3n
        (16n).isqrt()   // 4n
        (0n).isqrt()    // 0n
        (-1n).isqrt()   // throws
        ```
        
        Raises:
            InvalidArgument"""
    def pow(self, exp: int) -> int: ...
    async def pow_async(self, exp: int) -> int: ...
    def ilog(self, base: int) -> int:
        """Returns the integer logarithm of `self` in the given `base`, rounded
        down — i.e. the largest `n` such that `base ** n <= self`.
        
        Throws `InvalidArgument` if `self <= 0` or `base < 2`.
        
        # Examples
        ```
        (1000n).ilog(10n)   // 3n
        (1024n).ilog(2n)    // 10n
        (1n).ilog(10n)      // 0n
        (0n).ilog(10n)      // throws
        (10n).ilog(1n)      // throws
        ```
        
        Raises:
            InvalidArgument"""
    async def ilog_async(self, base: int) -> int:
        """Returns the integer logarithm of `self` in the given `base`, rounded
        down — i.e. the largest `n` such that `base ** n <= self`.
        
        Throws `InvalidArgument` if `self <= 0` or `base < 2`.
        
        # Examples
        ```
        (1000n).ilog(10n)   // 3n
        (1024n).ilog(2n)    // 10n
        (1n).ilog(10n)      // 0n
        (0n).ilog(10n)      // throws
        (10n).ilog(1n)      // throws
        ```
        
        Raises:
            InvalidArgument"""


class Bool(pydantic.BaseModel):
    ...


def _compare_shim(a: T, b: T, *, _types: dict[str, type]) -> int: ...
async def _compare_shim_async(a: T, b: T, *, _types: dict[str, type]) -> int: ...


def _float_total_cmp(a: float, b: float) -> int:
    """Three-way compares two floats by IEEE 754 `totalOrder` (`f64::total_cmp`).
    
    Internal shim backing `Comparable for float` (above) and kept bit-exact
    with the float comparator inside the native `_rust_sort`; not part of the
    public `Comparable`/`Sortable` surface."""
async def _float_total_cmp_async(a: float, b: float) -> int:
    """Three-way compares two floats by IEEE 754 `totalOrder` (`f64::total_cmp`).
    
    Internal shim backing `Comparable for float` (above) and kept bit-exact
    with the float comparator inside the native `_rust_sort`; not part of the
    public `Comparable`/`Sortable` surface."""


def _is_primitive_array(arr: typing.List[T], *, _types: dict[str, type]) -> bool: ...
async def _is_primitive_array_async(arr: typing.List[T], *, _types: dict[str, type]) -> bool: ...


def _rust_sort(arr: A, *, _types: dict[str, type]) -> A: ...
async def _rust_sort_async(arr: A, *, _types: dict[str, type]) -> A: ...


class Array(pydantic.BaseModel, typing.Generic[T]):
    @staticmethod
    def filled(length: int, value: T) -> typing.List[T]:
        """Builds a new array of `length` elements, each equal to `value`.
        
        This is the idiomatic way to allocate a runtime-sized, pre-initialized
        buffer — Fenwick trees, DP tables, adjacency lists — without a manual
        `while`/`push` loop. (Remember a 1-indexed structure needs `length + 1`
        slots.)
        
        # Parameters
        - `length`: the number of elements to create. A negative or zero `length`
          produces an empty array.
        - `value`: the value stored in every slot.
        
        # Warning
        `Array.filled` reuses the exact same `value` for every slot. For reference
        types (arrays, maps, class instances), every slot aliases the same object.
        Mutating one slot mutates all slots.
        
        Use a `while` loop to allocate independent mutable slots:
        ```
        let rows: int[][] = [];
        let i = 0;
        while (i < 3) {
          rows.push([]);
          i += 1;
        }
        ```
        
        # Returns
        A fresh `T[]` of length `max(length, 0)` with every element equal to
        `value`.
        
        Never throws.
        
        # Examples
        ```
        Array.filled(3, 0)     // [0, 0, 0]
        Array.filled(0, 0)     // []
        Array.filled(-1, 0)    // []
        Array.filled(2, "x")   // ["x", "x"]
        Array.filled(3, [])    // aliases the same inner array in all 3 slots
        ```"""
    @staticmethod
    async def filled_async(length: int, value: T) -> typing.List[T]:
        """Builds a new array of `length` elements, each equal to `value`.
        
        This is the idiomatic way to allocate a runtime-sized, pre-initialized
        buffer — Fenwick trees, DP tables, adjacency lists — without a manual
        `while`/`push` loop. (Remember a 1-indexed structure needs `length + 1`
        slots.)
        
        # Parameters
        - `length`: the number of elements to create. A negative or zero `length`
          produces an empty array.
        - `value`: the value stored in every slot.
        
        # Warning
        `Array.filled` reuses the exact same `value` for every slot. For reference
        types (arrays, maps, class instances), every slot aliases the same object.
        Mutating one slot mutates all slots.
        
        Use a `while` loop to allocate independent mutable slots:
        ```
        let rows: int[][] = [];
        let i = 0;
        while (i < 3) {
          rows.push([]);
          i += 1;
        }
        ```
        
        # Returns
        A fresh `T[]` of length `max(length, 0)` with every element equal to
        `value`.
        
        Never throws.
        
        # Examples
        ```
        Array.filled(3, 0)     // [0, 0, 0]
        Array.filled(0, 0)     // []
        Array.filled(-1, 0)    // []
        Array.filled(2, "x")   // ["x", "x"]
        Array.filled(3, [])    // aliases the same inner array in all 3 slots
        ```"""
    def length(self) -> int:
        """Returns the number of elements in the array."""
    async def length_async(self) -> int:
        """Returns the number of elements in the array."""
    def at(self, index: int) -> typing.Optional[T]:
        """Returns the element at `index`, or `null` if out of bounds.
        
        Negative indices count from the end: `-1` is the last element.
        
        # Examples
        ```
        [10, 20, 30].at(0)    // 10
        [10, 20, 30].at(-1)   // 30
        [10, 20, 30].at(99)   // null
        [10, 20, 30].at(-99)  // null
        ```"""
    async def at_async(self, index: int) -> typing.Optional[T]:
        """Returns the element at `index`, or `null` if out of bounds.
        
        Negative indices count from the end: `-1` is the last element.
        
        # Examples
        ```
        [10, 20, 30].at(0)    // 10
        [10, 20, 30].at(-1)   // 30
        [10, 20, 30].at(99)   // null
        [10, 20, 30].at(-99)  // null
        ```"""
    def push(self, item: T) -> int: ...
    async def push_async(self, item: T) -> int: ...
    def pop(self) -> typing.Optional[T]: ...
    async def pop_async(self) -> typing.Optional[T]: ...
    def remove_at(self, index: int) -> typing.Optional[T]: ...
    async def remove_at_async(self, index: int) -> typing.Optional[T]: ...
    def concat(self, other: typing.List[T]) -> typing.List[T]:
        """Returns a new array with the elements of `self` followed by the elements of `other`."""
    async def concat_async(self, other: typing.List[T]) -> typing.List[T]:
        """Returns a new array with the elements of `self` followed by the elements of `other`."""
    def for_each(self, fn: typing.Callable[[T], None], *, _types: dict[str, type]) -> None:
        """Calls `fn` once for each element in the array."""
    async def for_each_async(self, fn: typing.Callable[[T], None], *, _types: dict[str, type]) -> None:
        """Calls `fn` once for each element in the array."""
    def filter(self, predicate: typing.Callable[[T], bool], *, _types: dict[str, type]) -> typing.List[T]: ...
    async def filter_async(self, predicate: typing.Callable[[T], bool], *, _types: dict[str, type]) -> typing.List[T]: ...
    def filter_map(self, fn: typing.Callable[[T], typing.Optional[U]], *, _types: dict[str, type]) -> typing.List[U]:
        """Returns a new array containing the non-null results of `fn` applied to
        every element. The function is called once per element, left-to-right.
        Does not mutate `self`. For a lazy version, use `iter().filter_map(fn)`."""
    async def filter_map_async(self, fn: typing.Callable[[T], typing.Optional[U]], *, _types: dict[str, type]) -> typing.List[U]:
        """Returns a new array containing the non-null results of `fn` applied to
        every element. The function is called once per element, left-to-right.
        Does not mutate `self`. For a lazy version, use `iter().filter_map(fn)`."""
    def reverse(self) -> typing.List[T]:
        """Returns a new array with the elements in reverse order. Does not mutate `self`."""
    async def reverse_async(self) -> typing.List[T]:
        """Returns a new array with the elements in reverse order. Does not mutate `self`."""
    def sort_by(self, compare: typing.Callable[[T, T], int], *, _types: dict[str, type]) -> typing.List[T]: ...
    async def sort_by_async(self, compare: typing.Callable[[T, T], int], *, _types: dict[str, type]) -> typing.List[T]: ...
    def sort_by_key(self, key: typing.Callable[[T], U], *, _types: dict[str, type]) -> typing.List[T]: ...
    async def sort_by_key_async(self, key: typing.Callable[[T], U], *, _types: dict[str, type]) -> typing.List[T]: ...
    def slice(self, start: int, end: int) -> typing.List[T]:
        """Returns a sub-array from index `start` (inclusive) to `end` (exclusive).
        
        Negative indices count from the end. Out-of-range indices are clamped, and
        an `end` that resolves at or before `start` yields an empty array.
        
        # Examples
        ```
        [1, 2, 3, 4].slice(1, 3)   // [2, 3]
        [1, 2, 3].slice(-2, 3)     // [2, 3]
        [1, 2, 3].slice(2, 1)      // []
        ```"""
    async def slice_async(self, start: int, end: int) -> typing.List[T]:
        """Returns a sub-array from index `start` (inclusive) to `end` (exclusive).
        
        Negative indices count from the end. Out-of-range indices are clamped, and
        an `end` that resolves at or before `start` yields an empty array.
        
        # Examples
        ```
        [1, 2, 3, 4].slice(1, 3)   // [2, 3]
        [1, 2, 3].slice(-2, 3)     // [2, 3]
        [1, 2, 3].slice(2, 1)      // []
        ```"""
    def join(self, separator: str) -> str: ...
    async def join_async(self, separator: str) -> str: ...
    def map(self, f: typing.Callable[[T], U], *, _types: dict[str, type]) -> typing.List[U]: ...
    async def map_async(self, f: typing.Callable[[T], U], *, _types: dict[str, type]) -> typing.List[U]: ...
    def some(self, predicate: typing.Callable[[T], bool], *, _types: dict[str, type]) -> bool: ...
    async def some_async(self, predicate: typing.Callable[[T], bool], *, _types: dict[str, type]) -> bool: ...
    def every(self, predicate: typing.Callable[[T], bool], *, _types: dict[str, type]) -> bool: ...
    async def every_async(self, predicate: typing.Callable[[T], bool], *, _types: dict[str, type]) -> bool: ...
    def find(self, predicate: typing.Callable[[T], bool], *, _types: dict[str, type]) -> typing.Optional[T]: ...
    async def find_async(self, predicate: typing.Callable[[T], bool], *, _types: dict[str, type]) -> typing.Optional[T]: ...
    def find_index(self, predicate: typing.Callable[[T], bool], *, _types: dict[str, type]) -> typing.Optional[int]: ...
    async def find_index_async(self, predicate: typing.Callable[[T], bool], *, _types: dict[str, type]) -> typing.Optional[int]: ...
    def find_last(self, predicate: typing.Callable[[T], bool], *, _types: dict[str, type]) -> typing.Optional[T]: ...
    async def find_last_async(self, predicate: typing.Callable[[T], bool], *, _types: dict[str, type]) -> typing.Optional[T]: ...
    def find_last_index(self, predicate: typing.Callable[[T], bool], *, _types: dict[str, type]) -> typing.Optional[int]: ...
    async def find_last_index_async(self, predicate: typing.Callable[[T], bool], *, _types: dict[str, type]) -> typing.Optional[int]: ...
    def includes(self, item: T) -> bool: ...
    async def includes_async(self, item: T) -> bool: ...
    def index_of(self, item: T) -> typing.Optional[int]: ...
    async def index_of_async(self, item: T) -> typing.Optional[int]: ...
    def last_index_of(self, item: T) -> typing.Optional[int]: ...
    async def last_index_of_async(self, item: T) -> typing.Optional[int]: ...
    def reduce(self, reducer: typing.Callable[[A, T], A], initial: A, *, _types: dict[str, type]) -> A: ...
    async def reduce_async(self, reducer: typing.Callable[[A, T], A], initial: A, *, _types: dict[str, type]) -> A: ...
    def flat_map(self, f: typing.Callable[[T], typing.List[U]], *, _types: dict[str, type]) -> typing.List[U]: ...
    async def flat_map_async(self, f: typing.Callable[[T], typing.List[U]], *, _types: dict[str, type]) -> typing.List[U]: ...
    def clear(self) -> None: ...
    async def clear_async(self) -> None: ...
    def shift(self) -> typing.Optional[T]: ...
    async def shift_async(self) -> typing.Optional[T]: ...
    def unshift(self, item: T) -> int: ...
    async def unshift_async(self, item: T) -> int: ...
    def insert(self, item: T, idx: int) -> int:
        """Raises:
            InvalidArgument"""
    async def insert_async(self, item: T, idx: int) -> int:
        """Raises:
            InvalidArgument"""
    def splice(self, start: int, count: int, replace: typing.List[T]) -> None:
        """Raises:
            InvalidArgument"""
    async def splice_async(self, start: int, count: int, replace: typing.List[T]) -> None:
        """Raises:
            InvalidArgument"""


class Map(pydantic.BaseModel, typing.Generic[K, V]):
    def length(self) -> int:
        """Returns the number of entries in the map."""
    async def length_async(self) -> int:
        """Returns the number of entries in the map."""
    def has(self, key: K) -> bool: ...
    async def has_async(self, key: K) -> bool: ...
    def keys(self) -> typing.List[K]: ...
    async def keys_async(self) -> typing.List[K]: ...
    def values(self) -> typing.List[V]:
        """Returns an array of all values in the map. Order matches `keys()`."""
    async def values_async(self) -> typing.List[V]:
        """Returns an array of all values in the map. Order matches `keys()`."""
    def set(self, key: K, value: V) -> typing.Optional[V]: ...
    async def set_async(self, key: K, value: V) -> typing.Optional[V]: ...
    def get(self, key: K) -> typing.Optional[V]: ...
    async def get_async(self, key: K) -> typing.Optional[V]: ...
    def delete(self, key: K) -> typing.Optional[V]: ...
    async def delete_async(self, key: K) -> typing.Optional[V]: ...
    def get_or_insert(self, key: K, default: V) -> V: ...
    async def get_or_insert_async(self, key: K, default: V) -> V: ...
    def clear(self) -> None: ...
    async def clear_async(self) -> None: ...


def _to_string_default(value: T, *, _types: dict[str, type]) -> str: ...
async def _to_string_default_async(value: T, *, _types: dict[str, type]) -> str: ...


def _to_string_shim(value: T, *, _types: dict[str, type]) -> str: ...
async def _to_string_shim_async(value: T, *, _types: dict[str, type]) -> str: ...


def _cleanup_begin(value: T, *, _types: dict[str, type]) -> bool: ...
async def _cleanup_begin_async(value: T, *, _types: dict[str, type]) -> bool: ...


def _to_json_default(value: T, *, _types: dict[str, type]) -> baml.json.json:
    """Raises:
        JsonSerializationError"""
async def _to_json_default_async(value: T, *, _types: dict[str, type]) -> baml.json.json:
    """Raises:
        JsonSerializationError"""


def _to_json_shim(value: T, *, _types: dict[str, type]) -> baml.json.json:
    """Raises:
        JsonSerializationError"""
async def _to_json_shim_async(value: T, *, _types: dict[str, type]) -> baml.json.json:
    """Raises:
        JsonSerializationError"""


def _from_json_shim(j: baml.json.json, *, _types: dict[str, type]) -> T:
    """Raises:
        JsonDecodeError"""
async def _from_json_shim_async(j: baml.json.json, *, _types: dict[str, type]) -> T:
    """Raises:
        JsonDecodeError"""


def deep_copy(value: T, *, _types: dict[str, type]) -> T: ...
async def deep_copy_async(value: T, *, _types: dict[str, type]) -> T: ...


def deep_equals(a: T, b: T, *, _types: dict[str, type]) -> bool: ...
async def deep_equals_async(a: T, b: T, *, _types: dict[str, type]) -> bool: ...


class TaggedString(pydantic.BaseModel):
    parts: typing.List[str]
    values: typing.List[typing.Any]


class Float(pydantic.BaseModel):
    @staticmethod
    def parse(text: str) -> float:
        """Parses `text` as a floating-point number.
        
        Accepts decimal notation (`1.5`, `-0.25`), scientific notation
        (`1e3`, `-1.5E-3`), and the special tokens `inf` / `infinity` / `nan`
        (case-insensitive). An optional leading `+` or `-` sign is allowed.
        No surrounding whitespace, no underscore separators.
        
        Throws `ParseError` if `text` cannot be parsed. Note that successfully
        parsing `"nan"` gives a NaN value — use `result.is_nan()` to detect it
        rather than equality.
        
        # Examples
        ```
        float.parse("1.5")        // 1.5
        float.parse("-0.25")      // -0.25
        float.parse("1e3")        // 1000.0
        float.parse("inf")        // +infinity
        float.parse("NaN")        // NaN
        float.parse("")           // throws — empty
        float.parse("hello")      // throws — non-numeric
        float.parse(" 1.0 ")      // throws — whitespace not allowed; trim first
        ```
        
        Raises:
            ParseError"""
    @staticmethod
    async def parse_async(text: str) -> float:
        """Parses `text` as a floating-point number.
        
        Accepts decimal notation (`1.5`, `-0.25`), scientific notation
        (`1e3`, `-1.5E-3`), and the special tokens `inf` / `infinity` / `nan`
        (case-insensitive). An optional leading `+` or `-` sign is allowed.
        No surrounding whitespace, no underscore separators.
        
        Throws `ParseError` if `text` cannot be parsed. Note that successfully
        parsing `"nan"` gives a NaN value — use `result.is_nan()` to detect it
        rather than equality.
        
        # Examples
        ```
        float.parse("1.5")        // 1.5
        float.parse("-0.25")      // -0.25
        float.parse("1e3")        // 1000.0
        float.parse("inf")        // +infinity
        float.parse("NaN")        // NaN
        float.parse("")           // throws — empty
        float.parse("hello")      // throws — non-numeric
        float.parse(" 1.0 ")      // throws — whitespace not allowed; trim first
        ```
        
        Raises:
            ParseError"""
    @staticmethod
    def random() -> float: ...
    @staticmethod
    async def random_async() -> float: ...
    @staticmethod
    def pi() -> float:
        """Returns π, the ratio of a circle's circumference to its diameter,
        rounded to f64 precision (~15 decimal digits)."""
    @staticmethod
    async def pi_async() -> float:
        """Returns π, the ratio of a circle's circumference to its diameter,
        rounded to f64 precision (~15 decimal digits)."""
    @staticmethod
    def e() -> float:
        """Returns Euler's number `e`, the base of the natural logarithm."""
    @staticmethod
    async def e_async() -> float:
        """Returns Euler's number `e`, the base of the natural logarithm."""
    @staticmethod
    def golden_ratio() -> float:
        """Returns the golden ratio `φ = (1 + √5) / 2`, approximately `1.618`."""
    @staticmethod
    async def golden_ratio_async() -> float:
        """Returns the golden ratio `φ = (1 + √5) / 2`, approximately `1.618`."""
    @staticmethod
    def nan() -> float:
        """Returns a NaN value. Use `x.is_nan()` (not equality) to test for NaN."""
    @staticmethod
    async def nan_async() -> float:
        """Returns a NaN value. Use `x.is_nan()` (not equality) to test for NaN."""
    @staticmethod
    def inf() -> float:
        """Returns positive infinity. Use `-float.inf()` for negative infinity."""
    @staticmethod
    async def inf_async() -> float:
        """Returns positive infinity. Use `-float.inf()` for negative infinity."""
    def is_nan(self) -> bool:
        """Returns `true` if `self` is NaN (a "not a number" sentinel produced by
        invalid operations such as `(-1.0).sqrt()` or `float.inf() - float.inf()`).
        Note `0.0 / 0.0` throws `DivisionByZero` rather than producing NaN.
        
        NaN is the only float value that is not equal to itself (`x != x` iff
        `x.is_nan()`), so this predicate is the only correct way to test for NaN.
        
        # Examples
        ```
        (0.0).is_nan()         // false
        float.nan().is_nan()   // true
        float.inf().is_nan()   // false
        ```"""
    async def is_nan_async(self) -> bool:
        """Returns `true` if `self` is NaN (a "not a number" sentinel produced by
        invalid operations such as `(-1.0).sqrt()` or `float.inf() - float.inf()`).
        Note `0.0 / 0.0` throws `DivisionByZero` rather than producing NaN.
        
        NaN is the only float value that is not equal to itself (`x != x` iff
        `x.is_nan()`), so this predicate is the only correct way to test for NaN.
        
        # Examples
        ```
        (0.0).is_nan()         // false
        float.nan().is_nan()   // true
        float.inf().is_nan()   // false
        ```"""
    def is_infinite(self) -> bool:
        """Returns `true` if `self` is positive or negative infinity.
        
        # Examples
        ```
        float.inf().is_infinite()                 // true
        (-float.inf()).is_infinite()              // true
        (1000000.0 * 1000000.0).is_infinite()     // false  (1e12, large but finite)
        float.nan().is_infinite()                 // false
        ```"""
    async def is_infinite_async(self) -> bool:
        """Returns `true` if `self` is positive or negative infinity.
        
        # Examples
        ```
        float.inf().is_infinite()                 // true
        (-float.inf()).is_infinite()              // true
        (1000000.0 * 1000000.0).is_infinite()     // false  (1e12, large but finite)
        float.nan().is_infinite()                 // false
        ```"""
    def is_finite(self) -> bool:
        """Returns `true` if `self` is neither NaN nor infinite (i.e. an ordinary
        finite float, including subnormals and `±0.0`).
        
        # Examples
        ```
        (3.14).is_finite()         // true
        (0.0).is_finite()          // true
        float.inf().is_finite()    // false
        float.nan().is_finite()    // false
        ```"""
    async def is_finite_async(self) -> bool:
        """Returns `true` if `self` is neither NaN nor infinite (i.e. an ordinary
        finite float, including subnormals and `±0.0`).
        
        # Examples
        ```
        (3.14).is_finite()         // true
        (0.0).is_finite()          // true
        float.inf().is_finite()    // false
        float.nan().is_finite()    // false
        ```"""
    def abs(self) -> float:
        """Returns the absolute value of `self`. Preserves NaN.
        
        # Examples
        ```
        (-3.5).abs()           // 3.5
        (3.5).abs()            // 3.5
        (-0.0).abs()           // 0.0
        float.nan().abs()      // NaN
        ```"""
    async def abs_async(self) -> float:
        """Returns the absolute value of `self`. Preserves NaN.
        
        # Examples
        ```
        (-3.5).abs()           // 3.5
        (3.5).abs()            // 3.5
        (-0.0).abs()           // 0.0
        float.nan().abs()      // NaN
        ```"""
    def min(self, other: float) -> float:
        """Returns the smaller of `self` and `other`.
        
        **NaN handling:** if exactly one operand is NaN, returns the non-NaN one
        (ergonomic NaN suppression — matches Rust `f64::min`). If both are NaN,
        the result is NaN. Use `is_nan()` upstream if you need stricter handling.
        
        # Examples
        ```
        (3.0).min(5.0)              // 3.0
        (3.0).min(float.nan())      // 3.0  (NaN suppressed)
        float.nan().min(float.nan()) // NaN
        ```"""
    async def min_async(self, other: float) -> float:
        """Returns the smaller of `self` and `other`.
        
        **NaN handling:** if exactly one operand is NaN, returns the non-NaN one
        (ergonomic NaN suppression — matches Rust `f64::min`). If both are NaN,
        the result is NaN. Use `is_nan()` upstream if you need stricter handling.
        
        # Examples
        ```
        (3.0).min(5.0)              // 3.0
        (3.0).min(float.nan())      // 3.0  (NaN suppressed)
        float.nan().min(float.nan()) // NaN
        ```"""
    def max(self, other: float) -> float:
        """Returns the larger of `self` and `other`.
        
        NaN handling matches `min`: a non-NaN operand is preferred over NaN."""
    async def max_async(self, other: float) -> float:
        """Returns the larger of `self` and `other`.
        
        NaN handling matches `min`: a non-NaN operand is preferred over NaN."""
    def clamp(self, min: float, max: float) -> float:
        """Clamps `self` into the range `[min, max]`.
        
        Equivalent to `self.min(max).max(min)`. Callers should pass `min <= max`;
        if `min > max` the result is always `min` (the lower-clamp wins because
        it runs after the upper-clamp).
        
        NaN propagates through both `min` and `max` calls — if `self` is NaN,
        the result is NaN.
        
        # Examples
        ```
        (5.0).clamp(0.0, 10.0)     // 5.0
        (-3.0).clamp(0.0, 10.0)    // 0.0
        (15.0).clamp(0.0, 10.0)    // 10.0
        ```"""
    async def clamp_async(self, min: float, max: float) -> float:
        """Clamps `self` into the range `[min, max]`.
        
        Equivalent to `self.min(max).max(min)`. Callers should pass `min <= max`;
        if `min > max` the result is always `min` (the lower-clamp wins because
        it runs after the upper-clamp).
        
        NaN propagates through both `min` and `max` calls — if `self` is NaN,
        the result is NaN.
        
        # Examples
        ```
        (5.0).clamp(0.0, 10.0)     // 5.0
        (-3.0).clamp(0.0, 10.0)    // 0.0
        (15.0).clamp(0.0, 10.0)    // 10.0
        ```"""
    def floor(self) -> float:
        """Returns the largest integer ≤ `self`, as a float.
        Preserves the sign of `±0.0` and propagates NaN/±∞.
        
        # Examples
        ```
        (3.7).floor()    // 3.0
        (-1.5).floor()   // -2.0
        (3.0).floor()    // 3.0
        ```"""
    async def floor_async(self) -> float:
        """Returns the largest integer ≤ `self`, as a float.
        Preserves the sign of `±0.0` and propagates NaN/±∞.
        
        # Examples
        ```
        (3.7).floor()    // 3.0
        (-1.5).floor()   // -2.0
        (3.0).floor()    // 3.0
        ```"""
    def ceil(self) -> float:
        """Returns the smallest integer ≥ `self`, as a float.
        Preserves the sign of `±0.0` and propagates NaN/±∞.
        
        # Examples
        ```
        (3.2).ceil()    // 4.0
        (-1.5).ceil()   // -1.0
        ```"""
    async def ceil_async(self) -> float:
        """Returns the smallest integer ≥ `self`, as a float.
        Preserves the sign of `±0.0` and propagates NaN/±∞.
        
        # Examples
        ```
        (3.2).ceil()    // 4.0
        (-1.5).ceil()   // -1.0
        ```"""
    def round(self) -> float:
        """Returns the integer nearest to `self`, with ties rounded away from zero
        (so `0.5 → 1.0`, `-0.5 → -1.0`, `1.5 → 2.0`).
        
        # Examples
        ```
        (1.5).round()    // 2.0
        (-1.5).round()   // -2.0
        (1.4).round()    // 1.0
        (1.6).round()    // 2.0
        ```"""
    async def round_async(self) -> float:
        """Returns the integer nearest to `self`, with ties rounded away from zero
        (so `0.5 → 1.0`, `-0.5 → -1.0`, `1.5 → 2.0`).
        
        # Examples
        ```
        (1.5).round()    // 2.0
        (-1.5).round()   // -2.0
        (1.4).round()    // 1.0
        (1.6).round()    // 2.0
        ```"""
    def trunc(self) -> float:
        """Returns `self` truncated toward zero — i.e. the integer part with the
        fractional part discarded.
        
        # Examples
        ```
        (3.7).trunc()    // 3.0
        (-3.7).trunc()   // -3.0
        ```"""
    async def trunc_async(self) -> float:
        """Returns `self` truncated toward zero — i.e. the integer part with the
        fractional part discarded.
        
        # Examples
        ```
        (3.7).trunc()    // 3.0
        (-3.7).trunc()   // -3.0
        ```"""
    def fract(self) -> float:
        """Returns the fractional part of `self` (`self - self.trunc()`).
        
        # Examples
        ```
        (3.7).fract()    // 0.7
        (-3.7).fract()   // -0.7
        (5.0).fract()    // 0.0
        ```"""
    async def fract_async(self) -> float:
        """Returns the fractional part of `self` (`self - self.trunc()`).
        
        # Examples
        ```
        (3.7).fract()    // 0.7
        (-3.7).fract()   // -0.7
        (5.0).fract()    // 0.0
        ```"""
    def ifloor(self) -> int:
        """Like `floor`, but returns `int`. Throws if the floored value would not
        fit in `int` or `self` is NaN.
        
        Raises:
            InvalidArgument"""
    async def ifloor_async(self) -> int:
        """Like `floor`, but returns `int`. Throws if the floored value would not
        fit in `int` or `self` is NaN.
        
        Raises:
            InvalidArgument"""
    def iceil(self) -> int:
        """Like `ceil`, but returns `int`. Throws if the ceiled value would not fit
        in `int` or `self` is NaN.
        
        Raises:
            InvalidArgument"""
    async def iceil_async(self) -> int:
        """Like `ceil`, but returns `int`. Throws if the ceiled value would not fit
        in `int` or `self` is NaN.
        
        Raises:
            InvalidArgument"""
    def iround(self) -> int:
        """Like `round`, but returns `int`. Throws if the rounded value would not
        fit in `int` or `self` is NaN.
        
        Raises:
            InvalidArgument"""
    async def iround_async(self) -> int:
        """Like `round`, but returns `int`. Throws if the rounded value would not
        fit in `int` or `self` is NaN.
        
        Raises:
            InvalidArgument"""
    def itrunc(self) -> int:
        """Like `trunc`, but returns `int`. Throws if the integer part would not
        fit in `int` or `self` is NaN/±∞.
        
        Raises:
            InvalidArgument"""
    async def itrunc_async(self) -> int:
        """Like `trunc`, but returns `int`. Throws if the integer part would not
        fit in `int` or `self` is NaN/±∞.
        
        Raises:
            InvalidArgument"""
    def sqrt(self) -> float:
        """Returns the square root of `self`. For negative inputs returns NaN.
        
        # Examples
        ```
        (4.0).sqrt()       // 2.0
        (2.0).sqrt()       // 1.4142...
        (-1.0).sqrt()      // NaN
        (0.0).sqrt()       // 0.0
        ```"""
    async def sqrt_async(self) -> float:
        """Returns the square root of `self`. For negative inputs returns NaN.
        
        # Examples
        ```
        (4.0).sqrt()       // 2.0
        (2.0).sqrt()       // 1.4142...
        (-1.0).sqrt()      // NaN
        (0.0).sqrt()       // 0.0
        ```"""
    def pow(self, exp: float) -> float:
        """Returns `self ** exp` (floating-point exponentiation).
        
        Returns NaN for inputs where the mathematical value is not real (e.g.
        negative base raised to a non-integer power).
        
        # Examples
        ```
        (2.0).pow(10.0)         // 1024.0
        (2.0).pow(-1.0)         // 0.5
        (2.0).pow(0.5)          // sqrt(2)
        (-1.0).pow(0.5)         // NaN
        ```"""
    async def pow_async(self, exp: float) -> float:
        """Returns `self ** exp` (floating-point exponentiation).
        
        Returns NaN for inputs where the mathematical value is not real (e.g.
        negative base raised to a non-integer power).
        
        # Examples
        ```
        (2.0).pow(10.0)         // 1024.0
        (2.0).pow(-1.0)         // 0.5
        (2.0).pow(0.5)          // sqrt(2)
        (-1.0).pow(0.5)         // NaN
        ```"""
    def log(self, base: float) -> float:
        """Returns the logarithm of `self` in the given `base`. Equivalent to
        `self.ln() / base.ln()`.
        
        The boundary behavior follows directly from that formula:
        - `self < 0` → NaN  (since `ln(negative) = NaN`)
        - `self == 0`, `base > 0`, `base != 1` → `-∞`
        - `base < 0` → NaN
        - `base == 0`, `self > 0` → `±0` (sign opposite of `ln(self)`, since
          `±finite / -∞ = ∓0`)
        - `base == 1`, `self > 0`, `self != 1` → `±∞` (sign matches `ln(self)`,
          since `ln(1) == +0.0` and `±finite / +0.0 = ±∞`)
        - `base == 1`, `self == 1` → NaN (`0 / 0`)
        
        # Examples
        ```
        (1000.0).log(10.0)   // 3.0
        (8.0).log(2.0)       // 3.0
        (-1.0).log(10.0)     // NaN
        (0.0).log(10.0)      // -infinity
        (2.0).log(1.0)       // +infinity
        (1.0).log(1.0)       // NaN
        ```"""
    async def log_async(self, base: float) -> float:
        """Returns the logarithm of `self` in the given `base`. Equivalent to
        `self.ln() / base.ln()`.
        
        The boundary behavior follows directly from that formula:
        - `self < 0` → NaN  (since `ln(negative) = NaN`)
        - `self == 0`, `base > 0`, `base != 1` → `-∞`
        - `base < 0` → NaN
        - `base == 0`, `self > 0` → `±0` (sign opposite of `ln(self)`, since
          `±finite / -∞ = ∓0`)
        - `base == 1`, `self > 0`, `self != 1` → `±∞` (sign matches `ln(self)`,
          since `ln(1) == +0.0` and `±finite / +0.0 = ±∞`)
        - `base == 1`, `self == 1` → NaN (`0 / 0`)
        
        # Examples
        ```
        (1000.0).log(10.0)   // 3.0
        (8.0).log(2.0)       // 3.0
        (-1.0).log(10.0)     // NaN
        (0.0).log(10.0)      // -infinity
        (2.0).log(1.0)       // +infinity
        (1.0).log(1.0)       // NaN
        ```"""
    def hypot(self, other: float) -> float:
        """Returns the Euclidean distance `sqrt(self² + other²)`, computed in a way
        that avoids spurious overflow even when both magnitudes are large.
        
        # Examples
        ```
        (3.0).hypot(4.0)    // 5.0
        (0.0).hypot(0.0)    // 0.0
        ```"""
    async def hypot_async(self, other: float) -> float:
        """Returns the Euclidean distance `sqrt(self² + other²)`, computed in a way
        that avoids spurious overflow even when both magnitudes are large.
        
        # Examples
        ```
        (3.0).hypot(4.0)    // 5.0
        (0.0).hypot(0.0)    // 0.0
        ```"""
    def sin(self) -> float:
        """Sine of `self` (in radians)."""
    async def sin_async(self) -> float:
        """Sine of `self` (in radians)."""
    def cos(self) -> float:
        """Cosine of `self` (in radians)."""
    async def cos_async(self) -> float:
        """Cosine of `self` (in radians)."""
    def tan(self) -> float:
        """Tangent of `self` (in radians). Very large near odd multiples of π/2."""
    async def tan_async(self) -> float:
        """Tangent of `self` (in radians). Very large near odd multiples of π/2."""
    def asin(self) -> float:
        """Inverse sine, returning a value in `[-π/2, π/2]`.
        Returns NaN if `self` is outside `[-1, 1]`."""
    async def asin_async(self) -> float:
        """Inverse sine, returning a value in `[-π/2, π/2]`.
        Returns NaN if `self` is outside `[-1, 1]`."""
    def acos(self) -> float:
        """Inverse cosine, returning a value in `[0, π]`.
        Returns NaN if `self` is outside `[-1, 1]`."""
    async def acos_async(self) -> float:
        """Inverse cosine, returning a value in `[0, π]`.
        Returns NaN if `self` is outside `[-1, 1]`."""
    def atan(self) -> float:
        """Inverse tangent, returning a value in `(-π/2, π/2)`."""
    async def atan_async(self) -> float:
        """Inverse tangent, returning a value in `(-π/2, π/2)`."""
    def atan2(self, other: float) -> float:
        """Returns the angle (in radians) of the vector `(other, self)` — i.e. the
        angle from the positive x-axis to the point `(x = other, y = self)`.
        The result is in `[-π, π]`.
        
        # Examples
        ```
        (1.0).atan2(1.0)        // π/4    (45°)
        (1.0).atan2(0.0)        // π/2    (90°, due north)
        (0.0).atan2(-1.0)       // π      (180°, due west)
        ```"""
    async def atan2_async(self, other: float) -> float:
        """Returns the angle (in radians) of the vector `(other, self)` — i.e. the
        angle from the positive x-axis to the point `(x = other, y = self)`.
        The result is in `[-π, π]`.
        
        # Examples
        ```
        (1.0).atan2(1.0)        // π/4    (45°)
        (1.0).atan2(0.0)        // π/2    (90°, due north)
        (0.0).atan2(-1.0)       // π      (180°, due west)
        ```"""
    def sinh(self) -> float:
        """Hyperbolic sine."""
    async def sinh_async(self) -> float:
        """Hyperbolic sine."""
    def cosh(self) -> float:
        """Hyperbolic cosine."""
    async def cosh_async(self) -> float:
        """Hyperbolic cosine."""
    def tanh(self) -> float:
        """Hyperbolic tangent. Bounded to `(-1, 1)`."""
    async def tanh_async(self) -> float:
        """Hyperbolic tangent. Bounded to `(-1, 1)`."""
    def asinh(self) -> float:
        """Inverse hyperbolic sine. Domain: all reals."""
    async def asinh_async(self) -> float:
        """Inverse hyperbolic sine. Domain: all reals."""
    def acosh(self) -> float:
        """Inverse hyperbolic cosine. Domain: `[1, ∞)`. Returns NaN for `self < 1`."""
    async def acosh_async(self) -> float:
        """Inverse hyperbolic cosine. Domain: `[1, ∞)`. Returns NaN for `self < 1`."""
    def atanh(self) -> float:
        """Inverse hyperbolic tangent. Domain: `(-1, 1)`. Returns NaN outside that
        range, and ±∞ at the endpoints."""
    async def atanh_async(self) -> float:
        """Inverse hyperbolic tangent. Domain: `(-1, 1)`. Returns NaN outside that
        range, and ±∞ at the endpoints."""
    def to_radians(self) -> float:
        """Converts `self` from degrees to radians."""
    async def to_radians_async(self) -> float:
        """Converts `self` from degrees to radians."""
    def to_degrees(self) -> float:
        """Converts `self` from radians to degrees."""
    async def to_degrees_async(self) -> float:
        """Converts `self` from radians to degrees."""


class Int(pydantic.BaseModel):
    @staticmethod
    def parse(text: str) -> int:
        """Parses `text` as a base-ten signed integer.
        
        Accepts an optional leading `+` or `-` sign followed by one or more
        ASCII digits. No surrounding whitespace, no underscores, no other
        numeric formats. The result must fit in the `int` range (63-bit signed).
        
        Throws `ParseError` if `text` is empty, contains a non-digit character,
        or represents a value outside the `int` range.
        
        # Examples
        ```
        int.parse("42")       // 42
        int.parse("-7")       // -7
        int.parse("+0")       // 0
        int.parse("")         // throws — empty
        int.parse("12a")      // throws — non-digit
        int.parse(" 5 ")      // throws — whitespace not allowed; trim first
        int.parse("99999999999999999999")  // throws — out of range
        ```
        
        Raises:
            ParseError"""
    @staticmethod
    async def parse_async(text: str) -> int:
        """Parses `text` as a base-ten signed integer.
        
        Accepts an optional leading `+` or `-` sign followed by one or more
        ASCII digits. No surrounding whitespace, no underscores, no other
        numeric formats. The result must fit in the `int` range (63-bit signed).
        
        Throws `ParseError` if `text` is empty, contains a non-digit character,
        or represents a value outside the `int` range.
        
        # Examples
        ```
        int.parse("42")       // 42
        int.parse("-7")       // -7
        int.parse("+0")       // 0
        int.parse("")         // throws — empty
        int.parse("12a")      // throws — non-digit
        int.parse(" 5 ")      // throws — whitespace not allowed; trim first
        int.parse("99999999999999999999")  // throws — out of range
        ```
        
        Raises:
            ParseError"""
    @staticmethod
    def random(lower: int, upper: int) -> int:
        """Returns a uniformly distributed random integer in the half-open range
        `[lower, upper)`.
        
        Throws `InvalidArgument` if `lower >= upper` (the range would be empty).
        Uses the host's cryptographic entropy source — no user-supplied RNG
        handle. The largest representable half-open range is supported (e.g.
        `random(int.min_value(), int.max_value())` returns a value in
        `[int.min_value(), int.max_value())`; `int.max_value()` itself is never
        returned).
        
        # Examples
        ```
        int.random(0, 10)         // some value in {0, 1, ..., 9}
        int.random(-5, 5)         // some value in {-5, -4, ..., 4}
        int.random(0, 1)          // always 0  (single-element range)
        int.random(5, 5)          // throws — empty range
        int.random(10, 0)         // throws — lower > upper
        ```
        
        Raises:
            InvalidArgument"""
    @staticmethod
    async def random_async(lower: int, upper: int) -> int:
        """Returns a uniformly distributed random integer in the half-open range
        `[lower, upper)`.
        
        Throws `InvalidArgument` if `lower >= upper` (the range would be empty).
        Uses the host's cryptographic entropy source — no user-supplied RNG
        handle. The largest representable half-open range is supported (e.g.
        `random(int.min_value(), int.max_value())` returns a value in
        `[int.min_value(), int.max_value())`; `int.max_value()` itself is never
        returned).
        
        # Examples
        ```
        int.random(0, 10)         // some value in {0, 1, ..., 9}
        int.random(-5, 5)         // some value in {-5, -4, ..., 4}
        int.random(0, 1)          // always 0  (single-element range)
        int.random(5, 5)          // throws — empty range
        int.random(10, 0)         // throws — lower > upper
        ```
        
        Raises:
            InvalidArgument"""
    @staticmethod
    def max_value() -> int:
        """Returns the largest representable `int`, equal to `2^62 - 1`
        (`4_611_686_018_427_387_903`).
        
        Note: BAML integers are 63-bit signed (the runtime reserves one bit
        for the tagged-pointer Value encoding). Values outside the
        `[min_value(), max_value()]` range cannot round-trip through int."""
    @staticmethod
    async def max_value_async() -> int:
        """Returns the largest representable `int`, equal to `2^62 - 1`
        (`4_611_686_018_427_387_903`).
        
        Note: BAML integers are 63-bit signed (the runtime reserves one bit
        for the tagged-pointer Value encoding). Values outside the
        `[min_value(), max_value()]` range cannot round-trip through int."""
    @staticmethod
    def min_value() -> int:
        """Returns the smallest representable `int`, equal to `-2^62`
        (`-4_611_686_018_427_387_904`). Note `int.min_value().abs()` throws."""
    @staticmethod
    async def min_value_async() -> int:
        """Returns the smallest representable `int`, equal to `-2^62`
        (`-4_611_686_018_427_387_904`). Note `int.min_value().abs()` throws."""
    def abs(self) -> int:
        """Returns the absolute value of `self`.
        
        Throws `InvalidArgument` for `int.min_value()` because its absolute value
        (`2^62`) does not fit in an `int`.
        
        # Examples
        ```
        (-7).abs()         // 7
        (3).abs()          // 3
        (0).abs()          // 0
        int.min_value().abs()  // throws — overflow
        ```
        
        Raises:
            InvalidArgument"""
    async def abs_async(self) -> int:
        """Returns the absolute value of `self`.
        
        Throws `InvalidArgument` for `int.min_value()` because its absolute value
        (`2^62`) does not fit in an `int`.
        
        # Examples
        ```
        (-7).abs()         // 7
        (3).abs()          // 3
        (0).abs()          // 0
        int.min_value().abs()  // throws — overflow
        ```
        
        Raises:
            InvalidArgument"""
    def min(self, other: int) -> int:
        """Returns the smaller of `self` and `other`.
        
        # Examples
        ```
        (3).min(5)   // 3
        (3).min(3)   // 3
        (-2).min(0)  // -2
        ```"""
    async def min_async(self, other: int) -> int:
        """Returns the smaller of `self` and `other`.
        
        # Examples
        ```
        (3).min(5)   // 3
        (3).min(3)   // 3
        (-2).min(0)  // -2
        ```"""
    def max(self, other: int) -> int:
        """Returns the larger of `self` and `other`.
        
        # Examples
        ```
        (3).max(5)   // 5
        (3).max(3)   // 3
        (-2).max(0)  // 0
        ```"""
    async def max_async(self, other: int) -> int:
        """Returns the larger of `self` and `other`.
        
        # Examples
        ```
        (3).max(5)   // 5
        (3).max(3)   // 3
        (-2).max(0)  // 0
        ```"""
    def clamp(self, min: int, max: int) -> int:
        """Clamps `self` into the range `[min, max]`.
        
        Equivalent to `self.min(max).max(min)`. Callers should pass `min <= max`;
        if `min > max` the result is always `min` (the lower-clamp wins because
        it runs after the upper-clamp).
        
        # Examples
        ```
        (5).clamp(0, 10)    // 5
        (-3).clamp(0, 10)   // 0
        (15).clamp(0, 10)   // 10
        ```"""
    async def clamp_async(self, min: int, max: int) -> int:
        """Clamps `self` into the range `[min, max]`.
        
        Equivalent to `self.min(max).max(min)`. Callers should pass `min <= max`;
        if `min > max` the result is always `min` (the lower-clamp wins because
        it runs after the upper-clamp).
        
        # Examples
        ```
        (5).clamp(0, 10)    // 5
        (-3).clamp(0, 10)   // 0
        (15).clamp(0, 10)   // 10
        ```"""
    def isqrt(self) -> int:
        """Returns the integer square root of `self` — that is, the largest `int`
        `r` such that `r * r <= self`.
        
        Throws `InvalidArgument` if `self` is negative.
        
        # Examples
        ```
        (10).isqrt()   // 3
        (16).isqrt()   // 4
        (0).isqrt()    // 0
        (-1).isqrt()   // throws
        ```
        
        Raises:
            InvalidArgument"""
    async def isqrt_async(self) -> int:
        """Returns the integer square root of `self` — that is, the largest `int`
        `r` such that `r * r <= self`.
        
        Throws `InvalidArgument` if `self` is negative.
        
        # Examples
        ```
        (10).isqrt()   // 3
        (16).isqrt()   // 4
        (0).isqrt()    // 0
        (-1).isqrt()   // throws
        ```
        
        Raises:
            InvalidArgument"""
    def pow(self, exp: int) -> int:
        """Returns `self ** exp`. Saturates on overflow (positive overflow returns
        `int.max_value()`, negative overflow returns `int.min_value()`).
        
        `0 ** 0` returns `1`, by convention.
        
        If `exp` is negative the result is `0`, since the mathematical value
        `self ** -n = 1 / self ** n` is in `(-1, 1)` for `|self| > 1` and rounds
        to zero. (For `self == 1` or `self == -1` the value is `±1` which rounds
        to itself; this implementation still returns `0` for negative exponents
        uniformly.)
        
        # Examples
        ```
        (2).pow(10)      // 1024
        (2).pow(0)       // 1
        (0).pow(0)       // 1   (convention)
        (2).pow(-1)      // 0
        (10).pow(100)    // saturates to int.max_value()
        (-2).pow(3)      // -8
        ```"""
    async def pow_async(self, exp: int) -> int:
        """Returns `self ** exp`. Saturates on overflow (positive overflow returns
        `int.max_value()`, negative overflow returns `int.min_value()`).
        
        `0 ** 0` returns `1`, by convention.
        
        If `exp` is negative the result is `0`, since the mathematical value
        `self ** -n = 1 / self ** n` is in `(-1, 1)` for `|self| > 1` and rounds
        to zero. (For `self == 1` or `self == -1` the value is `±1` which rounds
        to itself; this implementation still returns `0` for negative exponents
        uniformly.)
        
        # Examples
        ```
        (2).pow(10)      // 1024
        (2).pow(0)       // 1
        (0).pow(0)       // 1   (convention)
        (2).pow(-1)      // 0
        (10).pow(100)    // saturates to int.max_value()
        (-2).pow(3)      // -8
        ```"""
    def ilog(self, base: int) -> int:
        """Returns the integer logarithm of `self` in the given `base`, rounded
        down — i.e. the largest `n` such that `base ** n <= self`.
        
        Throws `InvalidArgument` if `self <= 0` or `base < 2`.
        
        # Examples
        ```
        (1000).ilog(10)   // 3
        (1024).ilog(2)    // 10
        (1).ilog(10)      // 0
        (0).ilog(10)      // throws
        (10).ilog(1)      // throws
        ```
        
        Raises:
            InvalidArgument"""
    async def ilog_async(self, base: int) -> int:
        """Returns the integer logarithm of `self` in the given `base`, rounded
        down — i.e. the largest `n` such that `base ** n <= self`.
        
        Throws `InvalidArgument` if `self <= 0` or `base < 2`.
        
        # Examples
        ```
        (1000).ilog(10)   // 3
        (1024).ilog(2)    // 10
        (1).ilog(10)      // 0
        (0).ilog(10)      // throws
        (10).ilog(1)      // throws
        ```
        
        Raises:
            InvalidArgument"""
    def leading_zeros(self) -> int:
        """Returns the number of leading zero bits in the 64-bit two's-complement
        representation of `self`.
        
        # Examples
        ```
        (0).leading_zeros()  // 64
        (1).leading_zeros()  // 63
        (-1).leading_zeros() // 0  (all-ones)
        ```"""
    async def leading_zeros_async(self) -> int:
        """Returns the number of leading zero bits in the 64-bit two's-complement
        representation of `self`.
        
        # Examples
        ```
        (0).leading_zeros()  // 64
        (1).leading_zeros()  // 63
        (-1).leading_zeros() // 0  (all-ones)
        ```"""
    def leading_ones(self) -> int:
        """Returns the number of leading one bits in the 64-bit two's-complement
        representation of `self`.
        
        # Examples
        ```
        (0).leading_ones()   // 0
        (-1).leading_ones()  // 64
        ```"""
    async def leading_ones_async(self) -> int:
        """Returns the number of leading one bits in the 64-bit two's-complement
        representation of `self`.
        
        # Examples
        ```
        (0).leading_ones()   // 0
        (-1).leading_ones()  // 64
        ```"""
    def trailing_zeros(self) -> int:
        """Returns the number of trailing zero bits.
        
        # Examples
        ```
        (0).trailing_zeros()  // 64
        (8).trailing_zeros()  // 3   (binary 1000)
        (1).trailing_zeros()  // 0
        ```"""
    async def trailing_zeros_async(self) -> int:
        """Returns the number of trailing zero bits.
        
        # Examples
        ```
        (0).trailing_zeros()  // 64
        (8).trailing_zeros()  // 3   (binary 1000)
        (1).trailing_zeros()  // 0
        ```"""
    def trailing_ones(self) -> int:
        """Returns the number of trailing one bits.
        
        # Examples
        ```
        (7).trailing_ones()   // 3   (binary 111)
        (8).trailing_ones()   // 0
        (-1).trailing_ones()  // 64
        ```"""
    async def trailing_ones_async(self) -> int:
        """Returns the number of trailing one bits.
        
        # Examples
        ```
        (7).trailing_ones()   // 3   (binary 111)
        (8).trailing_ones()   // 0
        (-1).trailing_ones()  // 64
        ```"""
    def count_zeros(self) -> int:
        """Returns the total number of zero bits in the 64-bit two's-complement
        representation of `self`.
        
        # Examples
        ```
        (0).count_zeros()   // 64
        (-1).count_zeros()  // 0
        ```"""
    async def count_zeros_async(self) -> int:
        """Returns the total number of zero bits in the 64-bit two's-complement
        representation of `self`.
        
        # Examples
        ```
        (0).count_zeros()   // 64
        (-1).count_zeros()  // 0
        ```"""
    def count_ones(self) -> int:
        """Returns the total number of one bits in the 64-bit two's-complement
        representation of `self`. Also known as the population count or
        "popcount".
        
        # Examples
        ```
        (0).count_ones()   // 0
        (7).count_ones()   // 3
        (-1).count_ones()  // 64
        ```"""
    async def count_ones_async(self) -> int:
        """Returns the total number of one bits in the 64-bit two's-complement
        representation of `self`. Also known as the population count or
        "popcount".
        
        # Examples
        ```
        (0).count_ones()   // 0
        (7).count_ones()   // 3
        (-1).count_ones()  // 64
        ```"""


class Null(pydantic.BaseModel):
    ...


class String(pydantic.BaseModel):
    @staticmethod
    def from_(value: T, *, _types: dict[str, type]) -> str:
        """Renders any value as a human-readable string.
        
        If `value`'s runtime type implements `baml.ToString`, its `to_string`
        override is used; otherwise a default structural rendering is produced.
        Never throws.
        
        ```
        string.from(42)            // "42"
        string.from(true)          // "true"
        string.from([1, 2, 3])     // "[1, 2, 3]"
        ```
        
        Ideally this would read:
        
        ```
        if (value is baml.ToString) { value.to_string() }
        else { root._to_string_default(value) }
        ```
        
        but that does not work from the stdlib today. `is <interface>` and
        interface method dispatch are resolved at *compile time* by expanding the
        interface to its known implementor classes, and that implementor set is
        per-package: it is built from the package's dependencies + itself, never
        its dependents. `string.from` lives in the `baml` package, which a user's
        `implements baml.ToString` sits *above*, so from here the implementor set
        for `baml.ToString` is empty — the `is` test folds to constant `false` and
        the override never dispatches. (Same boundary `Sortable.sort`'s
        `_compare_shim` and `baml.json.to_json` work around.)
        
        So dispatch is resolved on `value`'s *runtime* class via
        `baml._to_string_shim` instead. TODO: once Kai's TIR rework lands (it moves
        interface conformance checks to runtime), replace this with the literal
        `is baml.ToString` form above and drop `_to_string_shim`."""
    @staticmethod
    async def from_async(value: T, *, _types: dict[str, type]) -> str:
        """Renders any value as a human-readable string.
        
        If `value`'s runtime type implements `baml.ToString`, its `to_string`
        override is used; otherwise a default structural rendering is produced.
        Never throws.
        
        ```
        string.from(42)            // "42"
        string.from(true)          // "true"
        string.from([1, 2, 3])     // "[1, 2, 3]"
        ```
        
        Ideally this would read:
        
        ```
        if (value is baml.ToString) { value.to_string() }
        else { root._to_string_default(value) }
        ```
        
        but that does not work from the stdlib today. `is <interface>` and
        interface method dispatch are resolved at *compile time* by expanding the
        interface to its known implementor classes, and that implementor set is
        per-package: it is built from the package's dependencies + itself, never
        its dependents. `string.from` lives in the `baml` package, which a user's
        `implements baml.ToString` sits *above*, so from here the implementor set
        for `baml.ToString` is empty — the `is` test folds to constant `false` and
        the override never dispatches. (Same boundary `Sortable.sort`'s
        `_compare_shim` and `baml.json.to_json` work around.)
        
        So dispatch is resolved on `value`'s *runtime* class via
        `baml._to_string_shim` instead. TODO: once Kai's TIR rework lands (it moves
        interface conformance checks to runtime), replace this with the literal
        `is baml.ToString` form above and drop `_to_string_shim`."""
    @staticmethod
    def from_utf8(utf8: bytes) -> str:
        """Decodes a `uint8array` of UTF-8 bytes into a string.
        
        Throws `InvalidArgument` if `utf8` is not valid UTF-8. To decode bytes
        with replacement characters (lossy), use `uint8array.to_string()`
        instead, which substitutes U+FFFD for invalid sequences.
        
        # Examples
        ```
        string.from_utf8(b"\\x68\\x69")          // "hi"
        string.from_utf8(b"\\xC3\\xA9")          // "é"
        string.from_utf8(b"\\xFF")              // throws — 0xFF is invalid UTF-8
        ```
        
        Raises:
            InvalidArgument"""
    @staticmethod
    async def from_utf8_async(utf8: bytes) -> str:
        """Decodes a `uint8array` of UTF-8 bytes into a string.
        
        Throws `InvalidArgument` if `utf8` is not valid UTF-8. To decode bytes
        with replacement characters (lossy), use `uint8array.to_string()`
        instead, which substitutes U+FFFD for invalid sequences.
        
        # Examples
        ```
        string.from_utf8(b"\\x68\\x69")          // "hi"
        string.from_utf8(b"\\xC3\\xA9")          // "é"
        string.from_utf8(b"\\xFF")              // throws — 0xFF is invalid UTF-8
        ```
        
        Raises:
            InvalidArgument"""
    @staticmethod
    def from_code_points(unicode: typing.List[int]) -> str:
        """Builds a string from an array of Unicode code points.
        
        Each value in `unicode` must be in the range `[0, 0x10FFFF]` and must
        not be a UTF-16 surrogate (the range `[0xD800, 0xDFFF]`). Throws
        `InvalidArgument` on any invalid value, identifying the offending
        position in the array.
        
        # Examples
        ```
        string.from_code_points([104, 105])         // "hi"
        string.from_code_points([233])              // "é"
        string.from_code_points([128017])           // "🐑"
        string.from_code_points([-1])               // throws — out of range
        string.from_code_points([55296])            // throws — surrogate (U+D800)
        ```
        
        Raises:
            InvalidArgument"""
    @staticmethod
    async def from_code_points_async(unicode: typing.List[int]) -> str:
        """Builds a string from an array of Unicode code points.
        
        Each value in `unicode` must be in the range `[0, 0x10FFFF]` and must
        not be a UTF-16 surrogate (the range `[0xD800, 0xDFFF]`). Throws
        `InvalidArgument` on any invalid value, identifying the offending
        position in the array.
        
        # Examples
        ```
        string.from_code_points([104, 105])         // "hi"
        string.from_code_points([233])              // "é"
        string.from_code_points([128017])           // "🐑"
        string.from_code_points([-1])               // throws — out of range
        string.from_code_points([55296])            // throws — surrogate (U+D800)
        ```
        
        Raises:
            InvalidArgument"""
    def length(self) -> int:
        """Returns the number of **Unicode code points** (characters) in the string.
        
        This is `O(1)` — the count is cached at construction time. For ASCII
        text it equals `byte_length()`, but for strings with multi-byte UTF-8
        characters they differ.
        
        # Examples
        ```
        "hello".length()  // 5
        "héllo".length()  // 5
        "😀".length()     // 1
        "".length()       // 0
        ```"""
    async def length_async(self) -> int:
        """Returns the number of **Unicode code points** (characters) in the string.
        
        This is `O(1)` — the count is cached at construction time. For ASCII
        text it equals `byte_length()`, but for strings with multi-byte UTF-8
        characters they differ.
        
        # Examples
        ```
        "hello".length()  // 5
        "héllo".length()  // 5
        "😀".length()     // 1
        "".length()       // 0
        ```"""
    def char_count(self) -> int:
        """Alias for `length()`."""
    async def char_count_async(self) -> int:
        """Alias for `length()`."""
    def byte_length(self) -> int:
        """Returns the length of the string in **UTF-8 bytes**.
        
        This is `O(1)`. Useful for serialization, network I/O, and buffer
        allocation where byte size matters.
        
        # Examples
        ```
        "hello".byte_length()  // 5
        "héllo".byte_length()  // 6
        "😀".byte_length()     // 4
        ```"""
    async def byte_length_async(self) -> int:
        """Returns the length of the string in **UTF-8 bytes**.
        
        This is `O(1)`. Useful for serialization, network I/O, and buffer
        allocation where byte size matters.
        
        # Examples
        ```
        "hello".byte_length()  // 5
        "héllo".byte_length()  // 6
        "😀".byte_length()     // 4
        ```"""
    def to_lower_case(self) -> str:
        """Returns the string with all Unicode characters converted to lowercase."""
    async def to_lower_case_async(self) -> str:
        """Returns the string with all Unicode characters converted to lowercase."""
    def to_upper_case(self) -> str:
        """Returns the string with all Unicode characters converted to uppercase."""
    async def to_upper_case_async(self) -> str:
        """Returns the string with all Unicode characters converted to uppercase."""
    def trim(self) -> str:
        """Returns the string with leading and trailing whitespace removed.
        
        Whitespace is defined per Unicode `White_Space` and includes ASCII spaces,
        tabs, newlines, and carriage returns as well as Unicode whitespace.
        
        # Examples
        ```
        "  hi  ".trim()    // "hi"
        "\\n\\thi\\n".trim()  // "hi"
        "hi".trim()        // "hi"
        ```"""
    async def trim_async(self) -> str:
        """Returns the string with leading and trailing whitespace removed.
        
        Whitespace is defined per Unicode `White_Space` and includes ASCII spaces,
        tabs, newlines, and carriage returns as well as Unicode whitespace.
        
        # Examples
        ```
        "  hi  ".trim()    // "hi"
        "\\n\\thi\\n".trim()  // "hi"
        "hi".trim()        // "hi"
        ```"""
    def trim_start(self) -> str:
        """Returns the string with leading whitespace removed (trailing whitespace
        is preserved). Whitespace follows Unicode `White_Space`.
        
        # Examples
        ```
        "  hi  ".trim_start()  // "hi  "
        "\\nhi".trim_start()    // "hi"
        ```"""
    async def trim_start_async(self) -> str:
        """Returns the string with leading whitespace removed (trailing whitespace
        is preserved). Whitespace follows Unicode `White_Space`.
        
        # Examples
        ```
        "  hi  ".trim_start()  // "hi  "
        "\\nhi".trim_start()    // "hi"
        ```"""
    def trim_end(self) -> str:
        """Returns the string with trailing whitespace removed (leading whitespace
        is preserved). Whitespace follows Unicode `White_Space`.
        
        # Examples
        ```
        "  hi  ".trim_end()  // "  hi"
        "hi\\n".trim_end()    // "hi"
        ```"""
    async def trim_end_async(self) -> str:
        """Returns the string with trailing whitespace removed (leading whitespace
        is preserved). Whitespace follows Unicode `White_Space`.
        
        # Examples
        ```
        "  hi  ".trim_end()  // "  hi"
        "hi\\n".trim_end()    // "hi"
        ```"""
    def includes(self, search: str) -> bool:
        """Returns true if `search` appears anywhere in the string."""
    async def includes_async(self, search: str) -> bool:
        """Returns true if `search` appears anywhere in the string."""
    def starts_with(self, prefix: str) -> bool:
        """Returns true if the string starts with `prefix`."""
    async def starts_with_async(self, prefix: str) -> bool:
        """Returns true if the string starts with `prefix`."""
    def ends_with(self, suffix: str) -> bool:
        """Returns true if the string ends with `suffix`."""
    async def ends_with_async(self, suffix: str) -> bool:
        """Returns true if the string ends with `suffix`."""
    def split(self, delimiter: str) -> typing.List[str]:
        """Splits the string by `delimiter` and returns an array of substrings.
        
        # Examples
        ```
        "a,b,c".split(",")     // ["a", "b", "c"]
        "hello".split("")      // ["h", "e", "l", "l", "o"]
        "no match".split(",")  // ["no match"]
        ```"""
    async def split_async(self, delimiter: str) -> typing.List[str]:
        """Splits the string by `delimiter` and returns an array of substrings.
        
        # Examples
        ```
        "a,b,c".split(",")     // ["a", "b", "c"]
        "hello".split("")      // ["h", "e", "l", "l", "o"]
        "no match".split(",")  // ["no match"]
        ```"""
    def chars(self) -> typing.List[str]:
        """Returns the string's Unicode code points as one-character strings.
        
        # Examples
        ```
        "hello".chars()  // ["h", "e", "l", "l", "o"]
        "é😀".chars()    // ["é", "😀"]
        "".chars()       // []
        ```"""
    async def chars_async(self) -> typing.List[str]:
        """Returns the string's Unicode code points as one-character strings.
        
        # Examples
        ```
        "hello".chars()  // ["h", "e", "l", "l", "o"]
        "é😀".chars()    // ["é", "😀"]
        "".chars()       // []
        ```"""
    def lines(self) -> typing.List[str]:
        """Splits the string into lines, recognizing both `\\n` and `\\r\\n` as line
        terminators. The terminator is **not** included in the returned strings.
        A final terminator does not produce a trailing empty string.
        
        # Examples
        ```
        "a\\nb\\nc".lines()    // ["a", "b", "c"]
        "a\\nb\\n".lines()     // ["a", "b"]      (no trailing empty)
        "a\\r\\nb".lines()     // ["a", "b"]      (CRLF handled)
        "".lines()           // []
        "\\n".lines()         // [""]
        ```"""
    async def lines_async(self) -> typing.List[str]:
        """Splits the string into lines, recognizing both `\\n` and `\\r\\n` as line
        terminators. The terminator is **not** included in the returned strings.
        A final terminator does not produce a trailing empty string.
        
        # Examples
        ```
        "a\\nb\\nc".lines()    // ["a", "b", "c"]
        "a\\nb\\n".lines()     // ["a", "b"]      (no trailing empty)
        "a\\r\\nb".lines()     // ["a", "b"]      (CRLF handled)
        "".lines()           // []
        "\\n".lines()         // [""]
        ```"""
    def substring(self, start: int, end: int) -> str:
        """Returns the substring between two **character** offsets `[start, end)`.
        
        Both `start` and `end` are codepoint indices, matching `length()`. Negative
        indices count from the end. The resolved offsets are clamped to
        `[0, length()]`, and an `end` that resolves at or before `start` yields an
        empty string. Never throws.
        
        # Examples
        ```
        "hello world".substring(0, 5)   // "hello"
        "hello".substring(2, 100)       // "llo" (end clamped)
        "hello".substring(-3, -1)       // "ll" (counts from the end)
        "😀hello".substring(0, 1)       // "😀"
        "😀hello".substring(1, 4)       // "hel"
        ```"""
    async def substring_async(self, start: int, end: int) -> str:
        """Returns the substring between two **character** offsets `[start, end)`.
        
        Both `start` and `end` are codepoint indices, matching `length()`. Negative
        indices count from the end. The resolved offsets are clamped to
        `[0, length()]`, and an `end` that resolves at or before `start` yields an
        empty string. Never throws.
        
        # Examples
        ```
        "hello world".substring(0, 5)   // "hello"
        "hello".substring(2, 100)       // "llo" (end clamped)
        "hello".substring(-3, -1)       // "ll" (counts from the end)
        "😀hello".substring(0, 1)       // "😀"
        "😀hello".substring(1, 4)       // "hel"
        ```"""
    def replace(self, search: str, replacement: str) -> str:
        """Replaces the first occurrence of `search` with `replacement`."""
    async def replace_async(self, search: str, replacement: str) -> str:
        """Replaces the first occurrence of `search` with `replacement`."""
    def index_of(self, search: str) -> typing.Optional[int]:
        """Returns the **character index** of the first occurrence of `search`, or
        `null` if not found.
        
        # Examples
        ```
        "hello world".index_of("world")  // 6
        "héllo".index_of("l")            // 2
        "😀hello".index_of("h")          // 1
        "abc".index_of("z")              // null
        ```"""
    async def index_of_async(self, search: str) -> typing.Optional[int]:
        """Returns the **character index** of the first occurrence of `search`, or
        `null` if not found.
        
        # Examples
        ```
        "hello world".index_of("world")  // 6
        "héllo".index_of("l")            // 2
        "😀hello".index_of("h")          // 1
        "abc".index_of("z")              // null
        ```"""
    def char_at(self, index: int) -> str: ...
    async def char_at_async(self, index: int) -> str: ...
    def code_point_at(self, index: int) -> int: ...
    async def code_point_at_async(self, index: int) -> int: ...
    def repeat(self, count: int) -> str:
        """Returns a new string that repeats `self` the given number of times.
        Negative counts are treated as 0.
        
        # Examples
        ```
        "ab".repeat(3)   // "ababab"
        "hi".repeat(0)   // ""
        "hi".repeat(-1)  // ""
        ```"""
    async def repeat_async(self, count: int) -> str:
        """Returns a new string that repeats `self` the given number of times.
        Negative counts are treated as 0.
        
        # Examples
        ```
        "ab".repeat(3)   // "ababab"
        "hi".repeat(0)   // ""
        "hi".repeat(-1)  // ""
        ```"""
    def matches(self, substring: str) -> bool:
        """Returns true if the string contains the given `substring`."""
    async def matches_async(self, substring: str) -> bool:
        """Returns true if the string contains the given `substring`."""
    def replace_all(self, search: str, replacement: str) -> str:
        """Replaces all occurrences of `search` with `replacement`."""
    async def replace_all_async(self, search: str, replacement: str) -> str:
        """Replaces all occurrences of `search` with `replacement`."""
    def is_numeric(self) -> bool:
        """Returns true if every character is numeric per Unicode (general categories
        `Nd`, `Nl`, `No` — decimal digits, letter-numbers, and other-numbers).
        Empty string returns true.
        
        # Examples
        ```
        "12345".is_numeric()  // true
        "Ⅷ".is_numeric()      // true (Roman numeral, Nl)
        "12a".is_numeric()    // false
        "".is_numeric()       // true (vacuous)
        ```"""
    async def is_numeric_async(self) -> bool:
        """Returns true if every character is numeric per Unicode (general categories
        `Nd`, `Nl`, `No` — decimal digits, letter-numbers, and other-numbers).
        Empty string returns true.
        
        # Examples
        ```
        "12345".is_numeric()  // true
        "Ⅷ".is_numeric()      // true (Roman numeral, Nl)
        "12a".is_numeric()    // false
        "".is_numeric()       // true (vacuous)
        ```"""
    def is_alphabetic(self) -> bool:
        """Returns true if every character is a Unicode letter (general category `L`).
        Empty string returns true.
        
        # Examples
        ```
        "héllo".is_alphabetic()  // true
        "漢字".is_alphabetic()    // true
        "abc1".is_alphabetic()   // false
        ```"""
    async def is_alphabetic_async(self) -> bool:
        """Returns true if every character is a Unicode letter (general category `L`).
        Empty string returns true.
        
        # Examples
        ```
        "héllo".is_alphabetic()  // true
        "漢字".is_alphabetic()    // true
        "abc1".is_alphabetic()   // false
        ```"""
    def is_alphanumeric(self) -> bool:
        """Returns true if every character is alphabetic OR numeric per Unicode.
        Empty string returns true. Equivalent to checking each char passes
        `is_alphabetic` or `is_numeric`.
        
        # Examples
        ```
        "abc123".is_alphanumeric()  // true
        "héllo7".is_alphanumeric()  // true
        "a b".is_alphanumeric()     // false (space is not)
        ```"""
    async def is_alphanumeric_async(self) -> bool:
        """Returns true if every character is alphabetic OR numeric per Unicode.
        Empty string returns true. Equivalent to checking each char passes
        `is_alphabetic` or `is_numeric`.
        
        # Examples
        ```
        "abc123".is_alphanumeric()  // true
        "héllo7".is_alphanumeric()  // true
        "a b".is_alphanumeric()     // false (space is not)
        ```"""
    def is_uppercase(self) -> bool:
        """Returns true if every character is uppercase per Unicode (general
        category `Lu`, plus other chars with the `Uppercase` property).
        Empty string returns true. Note: digits and most symbols are neither
        upper- nor lowercase.
        
        # Examples
        ```
        "HELLO".is_uppercase()  // true
        "Hello".is_uppercase()  // false
        "123".is_uppercase()    // false (digits aren't uppercase)
        ```"""
    async def is_uppercase_async(self) -> bool:
        """Returns true if every character is uppercase per Unicode (general
        category `Lu`, plus other chars with the `Uppercase` property).
        Empty string returns true. Note: digits and most symbols are neither
        upper- nor lowercase.
        
        # Examples
        ```
        "HELLO".is_uppercase()  // true
        "Hello".is_uppercase()  // false
        "123".is_uppercase()    // false (digits aren't uppercase)
        ```"""
    def is_lowercase(self) -> bool:
        """Returns true if every character is lowercase per Unicode (general
        category `Ll`, plus other chars with the `Lowercase` property).
        Empty string returns true.
        
        # Examples
        ```
        "hello".is_lowercase()  // true
        "Hello".is_lowercase()  // false
        ```"""
    async def is_lowercase_async(self) -> bool:
        """Returns true if every character is lowercase per Unicode (general
        category `Ll`, plus other chars with the `Lowercase` property).
        Empty string returns true.
        
        # Examples
        ```
        "hello".is_lowercase()  // true
        "Hello".is_lowercase()  // false
        ```"""
    def is_whitespace(self) -> bool:
        """Returns true if every character is whitespace per Unicode (`White_Space`
        property — includes space, tab, newline, NBSP, and other Unicode
        whitespace). Empty string returns true.
        
        # Examples
        ```
        "   ".is_whitespace()      // true
        " \\t\\n".is_whitespace()    // true
        "a b".is_whitespace()      // false
        ```"""
    async def is_whitespace_async(self) -> bool:
        """Returns true if every character is whitespace per Unicode (`White_Space`
        property — includes space, tab, newline, NBSP, and other Unicode
        whitespace). Empty string returns true.
        
        # Examples
        ```
        "   ".is_whitespace()      // true
        " \\t\\n".is_whitespace()    // true
        "a b".is_whitespace()      // false
        ```"""
    def is_control(self) -> bool:
        """Returns true if every character is a control character per Unicode
        (general category `Cc`). Empty string returns true.
        
        # Examples
        ```
        "\\n\\t".is_control()  // true
        "a".is_control()     // false
        ```"""
    async def is_control_async(self) -> bool:
        """Returns true if every character is a control character per Unicode
        (general category `Cc`). Empty string returns true.
        
        # Examples
        ```
        "\\n\\t".is_control()  // true
        "a".is_control()     // false
        ```"""
    def is_graphic(self) -> bool:
        """Returns true if every character is a "graphic" character — that is,
        not a control character and not whitespace. Empty string returns true.
        
        This is a convenience predicate for "visible/printing" characters. It
        uses the Unicode definition of control and whitespace.
        
        # Examples
        ```
        "abc".is_graphic()      // true
        "héllo".is_graphic()    // true
        "a b".is_graphic()      // false (space is not graphic)
        "a\\n".is_graphic()      // false (newline is control)
        ```"""
    async def is_graphic_async(self) -> bool:
        """Returns true if every character is a "graphic" character — that is,
        not a control character and not whitespace. Empty string returns true.
        
        This is a convenience predicate for "visible/printing" characters. It
        uses the Unicode definition of control and whitespace.
        
        # Examples
        ```
        "abc".is_graphic()      // true
        "héllo".is_graphic()    // true
        "a b".is_graphic()      // false (space is not graphic)
        "a\\n".is_graphic()      // false (newline is control)
        ```"""
    def is_ascii(self) -> bool:
        """Returns true if every character is ASCII (i.e. has a code point in
        `[0x00, 0x7F]`). Empty string returns true.
        
        # Examples
        ```
        "hello".is_ascii()    // true
        "héllo".is_ascii()    // false (`é` is U+00E9)
        ```"""
    async def is_ascii_async(self) -> bool:
        """Returns true if every character is ASCII (i.e. has a code point in
        `[0x00, 0x7F]`). Empty string returns true.
        
        # Examples
        ```
        "hello".is_ascii()    // true
        "héllo".is_ascii()    // false (`é` is U+00E9)
        ```"""
    def is_ascii_numeric(self) -> bool:
        """Returns true if every character is an ASCII decimal digit (`0`..=`9`).
        Empty string returns true. Stricter than `is_numeric`, which accepts
        non-ASCII numerals.
        
        # Examples
        ```
        "12345".is_ascii_numeric()  // true
        "Ⅷ".is_ascii_numeric()      // false (Roman numeral)
        ```"""
    async def is_ascii_numeric_async(self) -> bool:
        """Returns true if every character is an ASCII decimal digit (`0`..=`9`).
        Empty string returns true. Stricter than `is_numeric`, which accepts
        non-ASCII numerals.
        
        # Examples
        ```
        "12345".is_ascii_numeric()  // true
        "Ⅷ".is_ascii_numeric()      // false (Roman numeral)
        ```"""
    def is_ascii_alphabetic(self) -> bool:
        """Returns true if every character is an ASCII letter (`A`..=`Z` or
        `a`..=`z`). Empty string returns true."""
    async def is_ascii_alphabetic_async(self) -> bool:
        """Returns true if every character is an ASCII letter (`A`..=`Z` or
        `a`..=`z`). Empty string returns true."""
    def is_ascii_alphanumeric(self) -> bool:
        """Returns true if every character is an ASCII letter or digit. Empty
        string returns true."""
    async def is_ascii_alphanumeric_async(self) -> bool:
        """Returns true if every character is an ASCII letter or digit. Empty
        string returns true."""
    def is_ascii_uppercase(self) -> bool:
        """Returns true if every character is an ASCII uppercase letter
        (`A`..=`Z`). Empty string returns true."""
    async def is_ascii_uppercase_async(self) -> bool:
        """Returns true if every character is an ASCII uppercase letter
        (`A`..=`Z`). Empty string returns true."""
    def is_ascii_lowercase(self) -> bool:
        """Returns true if every character is an ASCII lowercase letter
        (`a`..=`z`). Empty string returns true."""
    async def is_ascii_lowercase_async(self) -> bool:
        """Returns true if every character is an ASCII lowercase letter
        (`a`..=`z`). Empty string returns true."""
    def is_ascii_whitespace(self) -> bool:
        """Returns true if every character is ASCII whitespace: space (`\\x20`),
        horizontal tab (`\\t`), line feed (`\\n`), form feed (`\\x0C`), or
        carriage return (`\\r`). Note this is narrower than `is_whitespace`,
        which accepts Unicode whitespace like NBSP."""
    async def is_ascii_whitespace_async(self) -> bool:
        """Returns true if every character is ASCII whitespace: space (`\\x20`),
        horizontal tab (`\\t`), line feed (`\\n`), form feed (`\\x0C`), or
        carriage return (`\\r`). Note this is narrower than `is_whitespace`,
        which accepts Unicode whitespace like NBSP."""
    def is_ascii_control(self) -> bool:
        """Returns true if every character is an ASCII control character — code
        points `[0x00, 0x1F]` plus DEL (`0x7F`). Empty string returns true."""
    async def is_ascii_control_async(self) -> bool:
        """Returns true if every character is an ASCII control character — code
        points `[0x00, 0x1F]` plus DEL (`0x7F`). Empty string returns true."""
    def is_ascii_graphic(self) -> bool:
        """Returns true if every character is an ASCII "graphic" character —
        printable, non-space, non-control: code points `[0x21, 0x7E]`. Note
        the ASCII space is *not* graphic. Empty string returns true."""
    async def is_ascii_graphic_async(self) -> bool:
        """Returns true if every character is an ASCII "graphic" character —
        printable, non-space, non-control: code points `[0x21, 0x7E]`. Note
        the ASCII space is *not* graphic. Empty string returns true."""
    def is_ascii_hex(self) -> bool:
        """Returns true if every character is an ASCII hexadecimal digit:
        `0`..=`9`, `a`..=`f`, or `A`..=`F`. Empty string returns true.
        
        # Examples
        ```
        "deadBEEF".is_ascii_hex()  // true
        "0x123".is_ascii_hex()     // false (`x` is not a hex digit)
        ```"""
    async def is_ascii_hex_async(self) -> bool:
        """Returns true if every character is an ASCII hexadecimal digit:
        `0`..=`9`, `a`..=`f`, or `A`..=`F`. Empty string returns true.
        
        # Examples
        ```
        "deadBEEF".is_ascii_hex()  // true
        "0x123".is_ascii_hex()     // false (`x` is not a hex digit)
        ```"""
    def to_utf8(self) -> bytes:
        """Returns the string encoded as a `uint8array` of UTF-8 bytes.
        
        The resulting byte array has length `self.byte_length()`. Inverse of the
        static `string.from_utf8(bytes)`.
        
        # Examples
        ```
        "hi".to_utf8()     // [0x68, 0x69]
        "é".to_utf8()      // [0xC3, 0xA9]
        "".to_utf8()       // []
        ```"""
    async def to_utf8_async(self) -> bytes:
        """Returns the string encoded as a `uint8array` of UTF-8 bytes.
        
        The resulting byte array has length `self.byte_length()`. Inverse of the
        static `string.from_utf8(bytes)`.
        
        # Examples
        ```
        "hi".to_utf8()     // [0x68, 0x69]
        "é".to_utf8()      // [0xC3, 0xA9]
        "".to_utf8()       // []
        ```"""
    def to_code_points(self) -> typing.List[int]:
        """Returns the string's Unicode code points as an array of `int`s.
        
        This is the exact inverse of `string.from_code_points`: for any string
        `s`, `string.from_code_points(s.to_code_points())` equals `s`. The result
        has one element per character (`self.length()` elements), each in
        `[0, 0x10FFFF]`. Never throws.
        
        Use this for char → integer mappings (checksums, base-N encoding, hashing,
        character classification) instead of indexing into a literal alphabet
        string.
        
        # Examples
        ```
        "hi".to_code_points()    // [104, 105]
        "é".to_code_points()     // [233]
        "🐑".to_code_points()    // [128017]
        "".to_code_points()      // []
        ```"""
    async def to_code_points_async(self) -> typing.List[int]:
        """Returns the string's Unicode code points as an array of `int`s.
        
        This is the exact inverse of `string.from_code_points`: for any string
        `s`, `string.from_code_points(s.to_code_points())` equals `s`. The result
        has one element per character (`self.length()` elements), each in
        `[0, 0x10FFFF]`. Never throws.
        
        Use this for char → integer mappings (checksums, base-N encoding, hashing,
        character classification) instead of indexing into a literal alphabet
        string.
        
        # Examples
        ```
        "hi".to_code_points()    // [104, 105]
        "é".to_code_points()     // [233]
        "🐑".to_code_points()    // [128017]
        "".to_code_points()      // []
        ```"""


class TypeValue(pydantic.BaseModel):
    def to_string(self) -> str: ...
    async def to_string_async(self) -> str: ...
    def _to_string_impl(self) -> str: ...
    async def _to_string_impl_async(self) -> str: ...
    def implements(self, other: None) -> bool: ...
    async def implements_async(self, other: None) -> bool: ...
    def implemented_by(self, other: None) -> bool: ...
    async def implemented_by_async(self, other: None) -> bool: ...
    def implementors(self) -> typing.List[None]: ...
    async def implementors_async(self) -> typing.List[None]: ...


class Uint8Array(pydantic.BaseModel):
    @staticmethod
    def zeroes(size: int) -> bytes:
        """Creates a new `uint8array` of the given size, filled with zeros.
        
        Throws an error if the `size` is out of range (e.g. negative).
        Panics if the allocation would cause an OOM.
        
        Raises:
            InvalidArgument"""
    @staticmethod
    async def zeroes_async(size: int) -> bytes:
        """Creates a new `uint8array` of the given size, filled with zeros.
        
        Throws an error if the `size` is out of range (e.g. negative).
        Panics if the allocation would cause an OOM.
        
        Raises:
            InvalidArgument"""
    @staticmethod
    def from_array(array: typing.List[int]) -> bytes:
        """Creates a `uint8array` from an array of integers.
        
        Throws `InvalidArgument` if any value is outside the range 0–255.
        
        Raises:
            InvalidArgument"""
    @staticmethod
    async def from_array_async(array: typing.List[int]) -> bytes:
        """Creates a `uint8array` from an array of integers.
        
        Throws `InvalidArgument` if any value is outside the range 0–255.
        
        Raises:
            InvalidArgument"""
    @staticmethod
    def from_hex(hex: str) -> bytes:
        """Decodes a hexadecimal string (e.g. `"deadbeef"`) into bytes.
        
        Throws `InvalidArgument` if the input contains non-hex characters or has an odd length.
        
        Raises:
            InvalidArgument"""
    @staticmethod
    async def from_hex_async(hex: str) -> bytes:
        """Decodes a hexadecimal string (e.g. `"deadbeef"`) into bytes.
        
        Throws `InvalidArgument` if the input contains non-hex characters or has an odd length.
        
        Raises:
            InvalidArgument"""
    @staticmethod
    def from_base64(base64: str) -> bytes:
        """Decodes a standard Base64-encoded string into bytes.
        
        Accepts both standard (`+/`) and URL-safe (`-_`) alphabets, with or without padding.
        
        # Examples
        ```
        uint8array.from_base64("aGVsbG8=")   // [104, 101, 108, 108, 111]  ("hello")
        ```
        
        Throws `InvalidArgument` if the input is not valid Base64.
        
        Raises:
            InvalidArgument"""
    @staticmethod
    async def from_base64_async(base64: str) -> bytes:
        """Decodes a standard Base64-encoded string into bytes.
        
        Accepts both standard (`+/`) and URL-safe (`-_`) alphabets, with or without padding.
        
        # Examples
        ```
        uint8array.from_base64("aGVsbG8=")   // [104, 101, 108, 108, 111]  ("hello")
        ```
        
        Throws `InvalidArgument` if the input is not valid Base64.
        
        Raises:
            InvalidArgument"""
    def length(self) -> int:
        """Returns the number of bytes in the array."""
    async def length_async(self) -> int:
        """Returns the number of bytes in the array."""
    def at(self, index: int) -> typing.Optional[int]:
        """Returns the byte at `index`, or `null` if out of bounds.
        
        Negative indices count from the end: `-1` is the last byte."""
    async def at_async(self, index: int) -> typing.Optional[int]:
        """Returns the byte at `index`, or `null` if out of bounds.
        
        Negative indices count from the end: `-1` is the last byte."""
    def push(self, item: int) -> int: ...
    async def push_async(self, item: int) -> int: ...
    def pop(self) -> typing.Optional[int]: ...
    async def pop_async(self) -> typing.Optional[int]: ...
    def concat(self, other: bytes) -> bytes:
        """Returns a new `uint8array` containing the concatenation of the two arrays."""
    async def concat_async(self, other: bytes) -> bytes:
        """Returns a new `uint8array` containing the concatenation of the two arrays."""
    def includes(self, item: int) -> bool:
        """Returns `true` if the array contains the given number.
        If the item is not a `u8`, this will always return `false`."""
    async def includes_async(self, item: int) -> bool:
        """Returns `true` if the array contains the given number.
        If the item is not a `u8`, this will always return `false`."""
    def reverse(self) -> bytes:
        """Returns a new `uint8array` with the bytes in reverse order."""
    async def reverse_async(self) -> bytes:
        """Returns a new `uint8array` with the bytes in reverse order."""
    def slice(self, start: int, end: int) -> bytes:
        """Returns a new `uint8array` with the bytes from `start` (inclusive) to
        `end` (exclusive).
        
        Negative indices count from the end. Out-of-range indices are clamped, and
        an `end` that resolves at or before `start` yields an empty array."""
    async def slice_async(self, start: int, end: int) -> bytes:
        """Returns a new `uint8array` with the bytes from `start` (inclusive) to
        `end` (exclusive).
        
        Negative indices count from the end. Out-of-range indices are clamped, and
        an `end` that resolves at or before `start` yields an empty array."""
    def to_array(self) -> typing.List[int]:
        """Returns the bytes as an array of integers, each in the range 0–255."""
    async def to_array_async(self) -> typing.List[int]:
        """Returns the bytes as an array of integers, each in the range 0–255."""
    def to_hex(self) -> str:
        """Encodes the bytes as a lowercase hexadecimal string (e.g. `"deadbeef"`)."""
    async def to_hex_async(self) -> str:
        """Encodes the bytes as a lowercase hexadecimal string (e.g. `"deadbeef"`)."""
    def to_base64(self) -> str:
        """Encodes the bytes as a standard Base64 string (with `=` padding).
        
        # Examples
        ```
        "hello".to_utf8().to_base64()   // "aGVsbG8="
        ```"""
    async def to_base64_async(self) -> str:
        """Encodes the bytes as a standard Base64 string (with `=` padding).
        
        # Examples
        ```
        "hello".to_utf8().to_base64()   // "aGVsbG8="
        ```"""
    def to_string(self) -> str: ...
    async def to_string_async(self) -> str: ...
    def _to_string_impl(self) -> str: ...
    async def _to_string_impl_async(self) -> str: ...
    def sort(self) -> None: ...
    async def sort_async(self) -> None: ...


__all__ = [
    "Bigint",
    "Bool",
    "Array",
    "Map",
    "deep_copy",
    "deep_copy_async",
    "deep_equals",
    "deep_equals_async",
    "TaggedString",
    "Float",
    "Int",
    "Null",
    "String",
    "TypeValue",
    "Uint8Array",
    "BamlError",
    "BamlPanic",
    "UNSET",
]
